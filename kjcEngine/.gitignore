# 빌드 산출물. 소스에서 build.bat 으로 다시 만들어지므로 담지 않는다
*.exe
*.obj
*.pdb
*.ilk

# 시작하다 막혔을 때 남기는 기록. 실행할 때마다 덮어쓴다
kjcEngine-error.txt

# 마지막으로 보던 자리. PC 마다 다르므로 담지 않는다
kjcEngine-view.txt

# 화면을 그대로 담은 것 (P 키). 볼 때마다 덮어쓴다
kjcEngine-shot.bmp

# 글꼴이 막혔을 때 남기는 기록
kjcEngine-font.txt

# 조절 창에서 담은 값. PC 마다 다르므로 담지 않는다
kjcEngine-shader.txt
