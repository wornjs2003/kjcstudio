# KJC Studio 프로젝트

## 어디를 보나 — 이 문서의 지도 (2026-09-22 지시)

**이 파일이 이천 줄을 넘었다.** 세션이 시작할 때 다 읽지만, 일하다 「이 자리는
어느 룰인가」 를 다시 찾을 때 헤맨다. 재권님이 **「세션별 룰이 또 흔들리고
각자 물어본다」** 고 하셔서 만들었다.

**먼저 볼 것 — 일 시작 전**

| 무엇을 알고 싶나 | 어느 절 |
|---|---|
| **내 레인이 어디인가** · 누가 무엇을 맡나 | 세션 역할 분담 |
| **내 폴더가 어디인가** · 서버 포트 | 세 자리로 나눈다 |
| 메인 폴더에서 조심할 것 | 메인 폴더는 셋이 함께 쓴다 |
| **고치기 전에 확인받나** | 작업 착수 규칙 |

**일하면서**

| 무엇을 알고 싶나 | 어느 절 |
|---|---|
| 색 · 선 · 칸 크기 · 스크롤바 | 디자인 규칙 |
| 값이 두 곳에 있을 때 | 같은 값은 한 곳에만 둔다 |
| **검사가 `0` 을 냈을 때** | 「없다」 와 「그 범위에는 없다」 를 가른다 |
| 화면을 띄워 보여드릴 때 | 작업 확인 규칙 |
| 서버 띄우기 · `.bat` 쓰기 | 실행 커맨드 |

**끝나고**

| 무엇을 알고 싶나 | 어느 절 |
|---|---|
| **커밋 · 푸시 · 배포 순서** | 커밋 · 푸시 · 배포 규칙 |
| 누가 올리나 | 레인마다 창구가 하나 있다 |
| 검수를 누가 보나 | 주식 작업은 두 세션이 검수한다 |
| **재권님께 여쭐 때** | 재권님이 확인하실 것은 맨 아래에 둔다 |

**룰을 만들거나 고칠 때** — 이 문서를 쓰는 법 · 룰은 재권님께 가기 전에 두 세션이 본다

**자주 걸리는 셋**

    `0` 이 나오면 **범위를 먼저 의심한다**
    **대보기 전에 재고**, 못 재면 예시로 쓰지 않는다
    묻는 것은 **맨 아래 한 자리에만** 둔다

## 기본 정보
- 회사명: KJC Studio
- 이메일: contact@kjcstudio.kr
- 전화: 02-1234-5678
- 주소: 서울특별시 강남구 테헤란로 123, KJC 빌딩 5층

## 사이트 성격
- 크리에이티브 스튜디오 포트폴리오 사이트
- 메인페이지(`index.html`)는 **세 층**이다 (2026-09-12 개편 · 2026-09-14 갤러리 복원)
  1. 풀스크린 인트로 — 첫 화면을 스튜디오 이름으로 꽉 채운다
  2. 카테고리 입구 목록 — 4개를 거대 타이포로. `data/categories.json` 이 채운다
  3. **대표 카테고리 작업물** — `featured` 카테고리의 갤러리. "전체 보기"로 그 카테고리로 간다
- 3층은 `gallery.js` 를 카테고리 페이지와 **그대로 재사용**한다.
  `body` 에 `data-category` 가 없으면 `categories.json` 의 `featured` 를 대표로 삼는 동작이라
  메인용 코드를 따로 두지 않는다
- 각 `category/*.html` 에는 그 카테고리 작업물 전체가 있다
- 2026-09-12 에 3층을 뺐다가 "메인에 보이던 항목이 사라졌다"는 지적을 받고 되살렸다.
  인트로·입구 목록은 그대로 두고 아래에 덧붙이는 형태다
- 참고 레퍼런스: https://0yanggang.imweb.me/21 (레이아웃만 차용, 색은 라이트로)

## 이 문서를 쓰는 법 — 검사에 걸리게 쓴다 (2026-09-16 지시)

재권님 말씀 그대로다.

> 기억해야 하는것은 안되고 검사해서 걸리는 방향이여야 할거같은데
> 기억하는게 아니고 검사하게 md 구성해야 한다

**룰을 적을 때마다 「이건 어떻게 걸리나」 를 함께 적는다.**

| | 예 | 어떻게 되나 |
|---|---|---|
| **걸리는 룰** | 경로가 틀렸다 · 값이 갈렸다 · 목록 개수가 안 맞는다 | 도구가 잡는다. **지켜진다** |
| **안 걸리는 룰** | "색을 박지 마라" · "먼저 알려라" · "순서를 지켜라" | 지키는 사람이 기억해야 한다. **안 지켜진다** |

- **검사하는 것이 있으면 그 이름을 적는다** — `python debugging/check.py` 처럼
- **없으면 「아직 검사가 없다」 고 적는다.** 검사가 없는 룰은 **언젠가 깨진다**는 뜻이고,
  적어두면 그것이 곧 도구를 만들 목록이 된다
- **검사 도구에 대상 값을 박지 않는다.** 세는 **방법**을 적는다.
  값을 박으면 대상이 바뀔 때마다 검사도 고쳐야 하고, **그 순간 검사가 또 하나의 복제가 된다**

### 룰은 재권님께 가기 전에 두 세션이 본다 (2026-09-18 지시)

재권님 말씀 그대로다.

> 룰잡으면 나한테 확인받기전에 항상 **홈페이지_정리 작업우선순위 섹션이랑
> 크로스 체크하고** 알려줘

    ① 룰 문안을 만든다
    ② `홈페이지_정리`(검증) · `작업우선순위`(감사) 에 보낸다
    ③ 답을 반영한다
    ④ **그 다음** 재권님께 확인받는다

**보낼 때 함께 적는다** — 무엇을 막으려는 룰인지 · 어디에 넣을지 ·
**아직 안 잰 것이 있으면 그것도.**

**두 세션은 의견을 내지 승인하지 않는다 (2026-09-18 · 작업우선순위 지적).**
「두 세션이 봤으니 됐다」 가 아니다. **정하는 것은 재권님이다.**
`작업우선순위` 는 「나르는 자리이지 허락하는 자리가 아니다」 가 이미 룰이고,
검토도 같은 선 안에 있다.

**검토는 두 세션 · 전파는 `개념정의`.** 이 절차로 두 세션이 룰을 **먼저 읽게**
되는데, 그것이 「전파도 같이」 로 흘러가면 「전파는 개념정의가 한다」 가 풀린다.
둘이 맡으면 서로 「저쪽이 했겠지」 가 된다 — 그래서 못박아 둔 것이다.

**왜** — 2026-09-18 하루에 내가 쓴 룰이 **세 번 틀렸다.**

    차트에만 걸어 둠        재권님이 찾으셨다 (「차트만 적용한건가?」)
    갈아끼우기·거르기 섞음   스케줄_툴개발이 자기 보드에 대보고 찾았다
    회차 전환 예시가 틀림    스케줄_툴개발이 재보니 0px 이었다

**셋 다 룰이 문서에 들어간 뒤에 드러났다.** 쓰는 쪽은 그 순간의 사례를
손에 들고 있어서 **너무 좁게 쓰거나 서로 다른 것을 한 이름으로 묶는다.**
자기 자리에 대보는 쪽이 있어야 갈린다.

**값을 재 보면 남는 장사다 (2026-09-18 · 작업우선순위 실측).**
그날 `CLAUDE.md` 계열 커밋 열하나 중 **셋이 앞 룰을 고치는 것**이었고,
**셋이 6분 사이에 연쇄로 났다.** 하나가 어긋나니 둘이 더 따라왔다.

    왕복 한 번   두 세션에 보내고 답을 받는다
    고치는 커밋   푸시 · 검증 · 전파가 전부 따라붙고,
                 그 사이 세션들이 **틀린 룰을 읽는다**

**「큰 룰만 거친다」 는 선을 긋지 않는다.** 그날 셋 중 둘이 「차트에만 걸어 둠」·
「예시가 틀림」 처럼 **작아 보이는 자리**에서 났다. 큰지 작은지를 쓰는 쪽이
판정하면 **그 판정이 또 하나의 판단**이 된다.

**「아직 안 쟀다」 를 그대로 문서에 넣지 않는다.** 세 번째가 그 모양이었다 —
받은 쪽이 「안 쟀다」 고 적어 보냈는데 내가 그대로 예시로 박았다.
**대보기 전에 재고, 못 재면 예시로 쓰지 않는다.**

### 세면 나오는 값은 본문에 적지 않는다 (2026-09-17)

**「지금 이렇다」 는 낡고, 「그때 그랬다」 는 안 낡는다.**

    적지 않는다   지금 도구가 아홉이다 · 오늘 13번 바뀌었다 · 통과 10
    괜찮다        2026-09-16 에 셋 vs 넷으로 갈렸다   ← 그날의 사건

- 개수를 말해야 하면 **세는 방법**을 적거나, **「열 번 넘게」 처럼 범위**로 적는다
- **「지금은 전부 있다」 같은 말도 낡는다.** 그 뒤에 하나가 더 생기면 거짓이 된다
- 날짜를 붙였다고 안전한 것이 아니다. **그날이 안 끝났으면 계속 늘어난다** —
  「2026-09-17 에 하루 13번」 이 그랬다

**이 파일에서 되풀이된 실수다.**

    「통과 10」         →  12
    「하지 않는 일 셋」  →  넷
    「하루 13번」       →  15

**전부 다른 세션이 찾았다.** 쓰는 쪽은 그 순간의 값을 손에 들고 있어서
낡는 것이 안 보인다. **이 목록도 늘어날 수 있다** — 그래서 개수를 세지 않고
적어만 둔다.

**왜** — 2026-09-16 에 `daily.css` 에 색이 직접 박혔다. 「색상 하드코딩 금지」 는
이 문서 맨 앞쪽에 있었고 읽었을 텐데도 박혔다. 그날 검사기를 고치면서 원인이 드러났는데,
**검사기가 파일 이름을 하나씩 적어 두어 새로 생긴 파일이 목록에 없었다.**
룰은 있었고 검사가 비어 있었다.

같은 날 재발 방지로 「`theme.css` 에 주석을 단다」 가 제안됐다가 **물러났다.**
주석은 읽어야 보이고, 그 사람은 실제로 `theme.css` 를 열어 찾다가 못 찾았다.
**주석을 달아도 찾는 이름이 다르면 여전히 안 보인다.**

### 「없다」 와 「그 범위에는 없다」 를 가른다 (2026-09-21)

**검사가 `0` 을 내면 범위를 먼저 의심한다.**

    정말 없다            →  0
    내가 본 자리에 없다    →  0      ← **출력이 같다**

**범위는 세 방향으로 어긋난다.**

| 범위가 | 무슨 일이 | 눈에 걸리나 |
|---|---|---|
| **좁으면** | 놓친다 | `0` 이라 한 번은 갸웃하게 된다 |
| **넓으면** | **남의 것을 센다** | 숫자가 나와서 **확인한 기분이 든다** |
| **바꿨는데 값이 같으면** | 우연일 수 있다 | **안 움직여서 제일 조용하다** |

**세 번째가 가장 위험하다.** 앞의 둘은 값이 움직여 눈에 걸리는데 이건 안 움직인다.
2026-09-21 에 검사 범위에서 `vendor` 필터가 빠졌는데 값이 **같게 나왔다** —
그 패턴이 `vendor` 에 없어서였다. **라이브러리가 하나 늘면 그날 갈린다.**
**「같으니 됐다」 는 「확인했다」 가 아니다.**

**무엇을 바꿔야 확인이 되나 — 셋이다.**

    범위를 바꾼다    다른 파일 · 다른 폴더 · 상위 디렉터리
    패턴을 바꾼다    따옴표 · 공백 · 대소문자 · 다른 이름
    방향을 바꾼다    「있는 것을 센다」 → **「없는 것을 센다」**

**세 번째가 잘 든다.** 2026-09-21 에 「`daily.html` 로 가는 「더보기」 가 0개」 를
뒤집어 `daily.html` 쪽을 세다가 **1** 을 찾았고, 그것이 메뉴라는 것까지 갈렸다.
**0 을 그대로 믿지 않고 반대쪽을 센 것이다.**

#### 파이프 뒤에서 종료코드를 읽지 않는다 (2026-09-21)

    검사 | tail | sed      →  `$?` 는 **`sed` 의 코드**다

**검사가 실패해도 `0` 이 나온다.** 이것도 「`0` 이 나왔는데 엉뚱한 것의 `0`」 이라
위와 같은 자리다.

**2026-09-21 에 두 세션이 같은 도구를 검증하며 각각 한 번씩 빠졌다.**
한 번은 **멀쩡한 도구를 고장 난 것으로 읽을 뻔했다** — 「크기가 달라도 0 이
나온다」 로 보였기 때문이다. 앞선 쪽이 뒤엣쪽에게 그 함정을 알려 준 뒤였는데도
몇 시간 뒤 같은 자리에 빠졌다.

**출력을 보고 싶은 마음이 검사를 망가뜨린다.** 둘 다 결과를 눈으로 보려고
파이프를 걸었다. **참으라고 하면 또 빠지므로, 보면서도 코드가 맞는 형태를 쓴다.**

    bash          검사 2>&1 | tail ; echo ${PIPESTATUS[0]}
    PowerShell    검사 > out.txt 2>&1     그 뒤에 파일을 본다

**`PIPESTATUS` 는 bash 전용이다.** 이 저장소는 두 셸을 함께 쓰므로 그 단서까지
적어 둔다 — 없으면 PowerShell 에서 **「룰대로 했는데 왜 안 되지」** 로 헤맨다.

#### 설명이 이미 있으면 의심할 계기조차 없다 (2026-09-22)

**같은 계열인데 한 가지가 다르다.**

    `0` 이 나왔다      **이상하다는 신호**가 있다. 한 번은 갸웃하게 된다
    설명이 이미 있다    **신호가 없다.** 화면과 맞아떨어져 보이면 「확인했다」 가 된다

**뒤쪽이 더 조용하다.**

**자책하는 방향의 숫자는 의심이 안 든다 (2026-09-22).**

    `0` 이 나왔다      「없을 리가」 하고 **갸웃하게 된다**
    199 가 나왔다      「역시 많이 어겼구나」 하고 **납득해 버린다**

그날 한 세션이 자기 위반을 세다 **199** 를 얻고 그대로 보고할 뻔했다. 세는
정규식이 `---` 만 보고 **박스 문자 `────` 를 못 봐서** 거의 전부가 위반으로
잡힌 것이었다. 다시 세니 **3** 이었고 그것도 전부 오탐이었다.
**「안 한 잘못을 시인할 뻔했다」 고 했다.**

**같은 날 나는 반대로 틀렸다.** 최근 마흔 개만 좁은 검색어로 세어 **1** 을
얻었는데, 전체를 넓게 세니 **29** 였다. **적게 나온 쪽도 의심해야 한다.**

**세는 표기가 세션마다 다르다.** 어떤 세션은 `---` 를, 어떤 세션은 `────` 를
쓴다. **남의 기록을 자기 잣대로 세면 통째로 어긋난다** — 「기록이 세션마다
따로라 자기 것만 셀 수 있다」 위에 이것이 하나 더 있다.

그날 한 세션이 **「멀티 조회가 시가총액·PER 을 안 준다」 는 주석을 읽고 그대로
믿었다.** 재보니 **그 주석이 틀렸다** — `per` · `pbr` · `marketCap` 이 다 왔다.

**낡은 주석 하나가 코드를 만들어냈다.** 그것을 근거로 단건을 따로 부르는 코드가
들어갔고, **필요 없는 호출이 종목마다 · 30초마다 돈다.**

**「이미 적혀 있다」 는 재본 것이 아니다.**

**기존 셋과 겹치지 않는다** — 자리가 다르다.

    「같은 값은 한 곳에만 둔다」         **쓰는 쪽** — 주석 대신 대조 도구를 만든다
    「검사 도구에 대상 값을 박지 않는다」  **만드는 쪽** — 박으면 낡는다
    **이것**                           **읽는 쪽** — 낡은 것을 읽었을 때

**룰을 하나 더 만드는 것도 복제다.** 그래서 절을 새로 세우지 않고 여기 붙인다 —
같은 날 사례가 셋 나왔는데 **둘은 위 절이 이미 덮고 있었다.**

### 교차 확인이 같은 범위면 확인이 아니다 (2026-09-21)

**절차는 지켰는데 결과가 안 나온 사례다.** 그래서 「남이 봐 주니까」 를 막는 자리다.

2026-09-21 에 룰 하나가 두 세션을 거쳤는데도 대상을 **하나 놓친 채** 문서에 들어갈
뻔했다. 보낸 쪽이 `holdings/index.html` 만 보고 세었고, **받은 쪽도 같은 범위로
확인**했다. **두 세션이 같은 창으로 두 번 본 셈이다.**

**범위를 바꾼 순간에 나왔다.** 옳아서 나온 것이 아니다.

- **어긋난 값을 그냥 두지 않는다.** 같은 날 세 번 다 이 방향에서 나왔다 —
  「같은 이야기인지 확인해 달라」 · 「왜 갈렸나」 · 「안 맞는데」
- **「확인해서 아무것도 안 나왔다」 를 실패로 세지 않는다.** 셋 중 하나는
  실제로 어긋난 것이 아니라 세는 범위가 달랐을 뿐이었는데, **확인하기
  전에는 그것도 몰랐다**
- **틀린 추정이 값을 낼 때가 있다.** 위 `vendor` 건은 한쪽이 원인을
  **틀리게** 짚어서 다시 쟀고, 그래서 진짜 원인이 나왔다. 맞았으면 닫혔다

**이것은 검사 범위를 폴더로 적는 것과 같은 이야기다.** 2026-09-16 에 색 검사기가
**파일 이름을 하나씩 적어 두어** `daily.css` 를 놓쳤고 폴더로 바꿨다.
**그때는 도구에서 났고, 2026-09-21 에는 사람에게서 났다.**

**도구가 적어둔 범위도 낡는다.** 같은 날 검증 절차서의 명령에 변수 이름 둘이
박혀 있어, 그대로 쓴 쪽이 엉뚱한 개수를 셌다. 이름을 안 박는 형태로 바꾸니
맞았다. **도구를 믿었는데 도구의 범위가 낡은 것이라, 의심할 생각조차 안 하게 된다**
(「검사 도구에 대상 값을 박지 않는다」 가 이미 룰인데 그 도구가 어기고 있었다).

**사례 — 숫자가 아니라 사실로 적는다.**

    적지 않는다   assets/js 에 그것이 14곳이다     ← 하루에 두 번 낡았다
    적는다        assets/js 를 **작은따옴표로 찾으면 0 이 나온다.**
                  거기는 큰따옴표를 쓴다

**구역마다 표기 관용이 반대다.** `holdings/js` 에서 통하던 검색어를 `assets/js` 에
쓰면 그냥 `0` 이다. **「범위」 는 폴더만이 아니라 표기 습관이기도 하다.**
응답 안에서도 갈린다 — 같은 날 어떤 값을 `data` 안에서만 찾아 「없다」 고 했는데
`meta` 에 있었다.

### 아직 검사가 없는 룰

만들면 이 목록에서 지운다. **검사할 수 있는데 안 만든 것**만 적는다 —
「먼저 알려라」 같은 것은 기계가 볼 수 없으므로 여기 적지 않는다.

| 룰 | 어떻게 검사할 수 있나 |
|---|---|
| `.bat` 안에는 한글을 쓰지 않는다 | `.bat` 파일에 ASCII 밖 글자가 있는지 본다 |
| 한글 출력 파이썬 도구에 인코딩 두 줄 | **한글을 찍고 `__main__` 이 있는** `*.py` 를 전부 보고, `reconfigure` **또는** `TextIOWrapper(sys.stdout` 가 있는지 본다 |
| HTTP 상태 줄에 한글 | `send_error(<숫자>, "<비ASCII>")` 를 찾는다. 그 줄은 latin-1 이라 **빈 응답**이 나간다 |
| 「N개는 예외」 라고 적힌 곳의 N ↔ 실제 | 예외를 세어 문서의 N 과 맞춘다 |
| 구역 `index.html` 이 `assets/css/common.css` 를 링크하는가 | 구역 폴더의 첫 화면에서 그 링크를 센다. 새 구역이 빠뜨리는 것을 잡는다 |
| 스크롤바를 `common.css` 밖에서 덮어쓰는 곳 | `::-webkit-scrollbar` 에 `width`·`background` 를 주는 곳을 찾는다 (감추는 `display:none`·`height:0` 은 뺀다) |
| 모달로 안 옮긴 「자세히」·「더보기」 | **`holdings/*.html` 전부**에서 `href` 는 있고 `data-modal` 이 없는 것을 센다. **0 이어야 한다** (`holdings/CLAUDE.md`) |
| 본문에서 물어 놓고 맨 아래 확인란이 없는 답변 | 세션 기록(`*.jsonl`)에서 「할까요·하시겠」 가 본문에 있는데 `───` 로 감싼 줄이 없는 것을 센다. **자기 기록만 셀 수 있다** |
| 확인란의 주소가 백틱으로 안 감싸진 것 | 세션 기록에서 `▶` 가 있는 줄의 백틱 없는 `http` 를 센다. **자기 기록만** |
| 확인란을 줄로 안 감싼 것 | `▶` 가 **줄 맨 앞**인 것만 센다. 룰 이야기를 하는 답변은 `▶` 를 **본문에서 언급**하는데(백틱 안 · 표 칸 안) 그것이 위반으로 세어진다 — 2026-09-22 에 한 세션이 `▶` 15개 중 하나가 그것이었다 |

**안 걸릴 줄 알았는데 걸린 것도 있다.** 「시킨 방식이 안 되면 말한다」 는
기계가 못 잡는다고 적어 두었는데, **값 굴리기 형태만은 잡혔다**
(`tools/hook-edit-churn.py`). **「못 잡는다」 고 단정하기 전에 한 번 더 본다.**

## 디자인 규칙
- 테마: **라이트 테마** (검은 배경 사용 금지 — 오래 보면 눈이 피로하고 기분이 가라앉는다, 2026-09-11 변경)
- 팔레트: **토스증권 팔레트로 네 구역 통일** (2026-09-12 변경)
  - 그 전에는 메인·설립 체크리스트가 `#fbfbfc`/`#3a6ae8`, holdings·projects 가 토스 값으로 갈려 있었다
  - 상단 메뉴로 오가는 화면끼리 바탕색이 미묘하게 달라 보여서 맞췄다
- 배경색: #f6f7f9 (순백 대신 옅은 회색 — 흰 카드가 떠 보이게)
- 카드/패널: #ffffff · 본문 글자: #101013 · 보조 글자: #4e5968
- 테두리: #f2f4f6 (거의 안 쓴다. 회색 바탕과 흰 카드의 대비로 면을 나눈다)
- **색상 하드코딩 금지.** 모든 색은 루트 `assets/css/theme.css` 에 변수로 정의하고
  `var(--…)` 로만 쓴다. 변수 한 곳만 고치면 전체 색이 바뀌어야 한다
- 예외로 어둡게 유지하는 것: 사진 위 캡션 막(`--scrim`), 이미지 확대 배경(`--overlay-bg`).
  밝게 바꾸면 사진 위 흰 글자가 안 읽힌다

### 색 바탕 위의 글씨 (2026-09-15 지시)

QA 보드에 **초록 바탕에 초록 글씨**가 나와 안 읽히는 곳이 있었다. 같은 화면 안에서도
어떤 칸은 검정이고 어떤 칸은 색이라 기준이 갈라져 있었다. 네 가지로 정한다.

**바탕에 색이 있으면 글씨는 그 색의 진한 버전. 바탕이 흰색이면 글씨는 검정.**

| 바탕 | 글씨 | 쓰는 곳 |
|---|---|---|
| **연한 색** (`--*-faint` · `--*-dim`, 10~12%) | **같은 색의 진한 값** | 배지 · 칩 · 상태 알약 |
| **진한 색** (100%) | **흰색** (`--kh-text-on-fill`) | 색을 꽉 채운 버튼·배지 |
| **흰색** | **검정** (`--*-text-primary`) | 카드 안 숫자·본문 |

    /* 이렇게 — 연한 초록 바탕에 진한 초록 글씨 */
    .pill.ok { background: var(--pj-done-faint); color: var(--pj-done); }

    /* 이렇게 — 흰 바탕이니 숫자는 검게. 상태는 초록 테두리가 알린다 */
    .stat-value.pass { color: var(--pj-text-primary); }

**왜** — 색 바탕에 검정을 얹으면 그 칸이 무슨 상태인지 색으로 읽히지 않는다.
반대로 흰 바탕에 색 글씨를 쓰면 화면이 어수선해진다.

**금지: 같은 농도끼리 겹치기.** 진한 초록 바탕에 진한 초록 글씨는 안 읽힌다.
농도를 벌린다 — 연한 바탕에 진한 글씨.

**점·막대처럼 글씨가 아닌 표시는 색을 그대로 쓴다.** 흰 바탕이라 글씨를
검정으로 둬도, 상태는 그 점으로 계속 읽힌다.

**단, 카드 테두리에는 색을 쓰지 않는다 (2026-09-15 지시).** 아래 「선」 절을 따른다.

### 보는 것을 바꿔도 자리는 그대로다 (2026-09-18 지시)

재권님 말씀이다.

> 투자자 정보 오늘 5일 로 바꿀때 **또 창크기 바뀌는데**
> 이거 룰 체크 제대로 해서 하라고 했잖아 **차트만 적용한건가?**

**차트에만 적용했다.** 룰을 「차트의 세로 범위」 로 좁게 써서, 같은 문제가
차트 밖에서 또 났다. **탭·단추로 내용만 바꿀 때 칸 크기가 바뀌면 안 된다.**

    투자자 정보   오늘 → 한 줄 · 5일 → 다섯 줄     칸 높이를 안 잡아 뒀다
    차트 지표     켜면 봉 크기가 바뀌었다           세 번 깨졌다

**왜** — 칸이 커졌다 작아지면 **아래 것들이 전부 밀린다.** 무엇이 바뀐 것인지
읽을 수 없고, 누르려던 자리가 움직인다. 「보는 것을 바꾼다」 는 **내용을 바꾸는
것이지 화면을 바꾸는 것이 아니다.**

**어떻게** — 칸 높이를 미리 잡거나(`min-height`), **가장 큰 내용에 맞춘다.**
줄 수가 다른 것을 오갈 때는 적은 쪽에 빈 자리를 둔다.

**무엇이 검사하나** — 둘이다. 보는 자리가 다르다.

    python tools/check-layout.py      **고치기 전후**를 견준다
    holdings/js/chart.js 의 checkScale  **화면이 도는 동안** 차트 세로를 본다

앞의 것이 2026-09-21 에 생겼다. 재권님 말씀이 계기다 — **「뭐 수정할때마다
창 크기 바뀌는데 재발 안하게좀 해. 창 크기 재서 코드나 스크립트에 박아놓고
안바뀌게 해줘」.** 룰은 2026-09-18 부터 있었는데 **검사가 없어서 세 번 재발했다**
(차트 · 투자자 정보 · 첫 화면).

    python tools/check-layout.py --save   지금 화면을 기준으로 삼는다
    python tools/check-layout.py          기준과 대보고, 어긋나면 어느 칸인지 낸다

**높이만 본다.** 2026-09-21 실측 — 칸 310개를 연달아 두 번 재니 **높이는 전부
그대로였고 폭만 넷이 움직였다.** 종목 이름·가격의 자릿수가 실시간으로 달라져서다.
**폭을 함께 보면 헛경보가 난다.** 재권님이 말씀하신 「창 크기」 도 세로 이야기다.

**내부 스크롤 칸의 안은 재지 않는다.** 순위표는 줄이 몇 개든 문서 높이가 안
움직인다 — 스크롤 칸 안에 있어서다. 같은 날 실측으로 **그 칸이 10597px 인데
문서 전체는 1237px** 이었다. 거기까지 기준으로 박으면 데이터가 바뀔 때마다 걸린다.

**그래서 이 도구가 안 보는 자리가 있다.**

    덮는다     창 크기 · 스크롤 칸 **자체**의 크기
    안 덮는다   스크롤 칸 **안**의 크기  →  **사람이 본다**

**낡아서 안 보는 것이 아니라 처음부터 그렇게 설계한 것이라 더 잊히기 쉽다.**
2026-09-21 에 「투자자 정보 그래프를 2/3 로」 라는 지시가 정확히 그 자리였다.
`#kh-iv-box` 가 172 → 133 이 됐는지를 **도구는 못 본다** — 그 칸이 천장
(`max-height: 690px`) 안에 있어서다. 다른 세션이 직접 재서 확인했다.

**「검사가 있으니 됐다」 로 넘어가지 않는다.** 도구가 덮는 것과 눈으로 볼 것을
가르는 줄이 여기다. 천장이 듣는지는 도구가 본다(`kh-card#2` 가 706 → 690).
**천장 안에서 무엇이 얼마가 됐는지는 사람이 본다.**

**칸 이름은 `id` 가 있으면 `id` 를 쓴다.** 세션끼리 그 이름으로 부르기 때문이다.
같은 날 `class` 만 보다가, 다른 세션이 `#kh-iv-box` 라고 넘겨준 칸을 도구는
`kh-idxp-b` 로 불렀다. **재고 있는데도 「기준에 없다」 로 읽혔다.**

**커밋 뒤에 알린다.** `holdings/` 의 `.css`·`.html`·`.js` 가 담기면
`.githooks/post-commit` 이 돌려 보라고 알린다. **훅이 직접 돌리지는 않는다** —
크롬을 띄워 네 화면을 재는 데 1분 넘게 걸리고 8765 가 떠 있어야 한다.

**깊이 3까지만 본다 — 그 밖은 「없다」 가 아니라 「안 봤다」 로 낸다.**
2026-09-21 에 한 세션이 「자세히 ›」 를 머리줄 안으로 옮기자 도구가
**「칸이 끝내 안 나왔습니다」** 로 냈다. **화면에는 멀쩡히 있었고 깊이 4로
밀려난 것**이었다. 그 세션은 자기 CSS 가 잘못된 줄 알고 HTML·CSS 응답을
두 번 확인했다.

**깊이를 늘리는 것이 답이 아니다.** 6 으로 늘려 보았더니 **작은 글자 칸이
흔들렸다**(`kh-mut` 20 → 17px). **늘리면 헛경보, 안 늘리면 놓친다.**

**그래서 「없는지」 를 화면에 직접 물어본다.** 깊이 제한과 무관하게
`querySelector` 로 확인해 **「깊이 밖입니다(화면에는 있습니다)」** 와
**「끝내 안 나왔습니다」** 를 가른다. 앞은 **실패가 아니라 기준이 낡은 것**이라
`--save` 로 다시 잡으면 된다.

**「`0` 이 나오면 범위를 먼저 의심한다」 의 도구 쪽 사례다** — 같은 절의
「도구가 적어둔 범위도 낡는다」 가 여기에 걸린다.

**스크롤바를 숨기지 않고 잰다.** 숨기면 콘텐츠 폭이 실제보다 넓게 재지고
「(스크롤바 폭)」 항목이 늘 `0` 이 되어 뜻을 잃는다. 두면 재권님 화면과
조건이 가깝고, **덤으로 「스크롤바는 한 벌만 쓴다」 의 6px 이 깨지는 것도 잡는다** —
2026-09-21 에 네 화면 전부 6px 으로 들어갔다.

**기준을 어떤 조건에서 쟀는지 파일에 함께 적는다** (창 크기 · 헤드리스 여부 · 잰 날).
**헤드리스와 실제 창은 값이 다를 수 있다** — 같은 조건끼리만 견줘야 뜻이 있다.
창 크기를 바꾸면 도구가 **기준을 다시 잡으라고 멈춘다.**

**「갈아 끼우기」 와 「거르기」 를 가른다 (2026-09-18 · 스케줄_툴개발 지적).**

| | 무엇 | 예 | 어떻게 |
|---|---|---|---|
| **갈아 끼우기** | 같은 자리에 **다른 내용**을 넣는다 | 오늘 ↔ 5일 · 차트 지표 | **높이를 잡는다** |
| **거르기** | 보여줄 **개수**를 줄인다 | 필터 칩 · 검색 | **줄어드는 것이 맞다** |

**거르기에 높이를 잡으면 더 나빠진다.** 「불일치만」 으로 걸러 한 줄이 남는데
가장 큰 내용(15줄)에 맞춰 두면 **빈 칸이 14줄 생긴다.** 막으려던 것의 반대다.

**펼치기는 또 다르다.** 아코디언처럼 **눌러서 여는 것**은 늘어나는 것이
누른 사람이 시킨 일이다. 2026-09-18 에 Debugging 보드의 회차 전환을
갈아 끼우기로 보고 예시에 넣었다가 **실측에서 차이 0px 로 나와 뺐다** —
`min-height` 가 없다는 것만 보고 「걸린다」 로 건너뛴 것이었다.

**가르는 기준 한 줄 — 누른 것과 일어난 일이 맞는가.**

| 무엇을 눌렀나 | 무엇이 일어나야 하나 | 어긋나면 |
|---|---|---|
| **다르게 보려고** 눌렀다 | 내용만 바뀐다 | **자리까지 움직이면 문제다** |
| **줄이려고** 눌렀다 | 줄어든다 | 안 줄면 문제다 |
| **열려고** 눌렀다 | 늘어난다 | 안 늘면 문제다 |

**아래가 밀리는 것 자체는 기준이 아니다.** 아코디언을 열면 아래가 밀리는데
그것은 **연 사람이 시킨 일**이다. 문제는 **안 시켰는데 움직이는 것**이다.

**누르지 않았는데 움직이는 것**은 이 룰 밖이다 — 칸을 새로 넣어 화면이
길어지는 것은 배치를 바꾼 것이지 「보는 것을 바꾼」 것이 아니다.

**대보기 전에 재고 나서 적는다.** 위 셋은 겉으로 비슷해 보이는데 실제로
움직이는 양이 다르다.

**이 룰은 네 구역 전부에 해당한다.**

### 스크롤바는 한 벌만 쓴다 (2026-09-18 지시)

재권님 말씀이다.

> UI 에 사용되는 스크롤바는 **하나로만 적용**되도록 룰잡아 그리고
> **위 아래 화살표 잇는데 이건 없애주고** 지금 스크롤 바의 가로 길이의
> **2/3** 으로 제작되도록해줘

**모양은 한 곳에서만 정한다.** 구역 CSS 에서 굵기나 색을 덮어쓰지 않는다 —
「같은 값은 한 곳에만 둔다」 와 같다.

**그 「한 곳」 은 `assets/css/common.css` 다 (2026-09-18 지시).**
**모든 구역 화면이 각자 링크한다.** 만들기 전에는 아래 문제가 그대로다.

    <link rel="stylesheet" href="../assets/css/common.css">

**왜 새 파일인가** — `theme.css` 는 변수 파일이고 `nav.css` 는 메뉴 파일이라,
둘 중 하나에 넣으면 **파일 이름과 내용이 어긋나 다음 사람이 못 찾는다.**
`nav.css` 가 이미 그 꼴이다(아래).

**약점은 「새 구역이 생기면 링크를 빠뜨린다」 는 것**이고, 그것은
**검사로 잡는다** — 구역 `index.html` 이 이 파일을 링크하는지 센다.

**왜 이 자리가 필요했나 (2026-09-18 실측)** — `assets/css/base.css` 에
넣었더니 **구역에는 안 닿았다.**

    base.css 를 싣는 것    메인 화면들뿐 — index · about · services · contact · category/*
    구역이 읽는 공용        **theme.css · nav.css 둘뿐**
                           holdings · company-setup · projects · ai-work · debugging · api-board

**「구역 공통 기본 규칙」 이 갈 자리가 아예 없다.** `theme.css` 는 변수 파일이고
`nav.css` 는 메뉴 파일이다. 그래서 **공통 규칙이 생길 때마다 메뉴 파일에 밀어
넣게 된다** — `nav.css:31` 에 그 흔적이 있다.

    /* 메뉴가 스스로 챙기는 리셋 — base.css 에 링크 밑줄과 버튼 테두리를
       지우는 규칙이 있는데, **그 파일을 싣는 것은 메인 사이트 하나뿐이다** */

**스크롤바가 두 번째 사례다.** 그래서 `common.css` 를 만든다.

    굵기        **6px**
    화살표      **없앤다** — `::-webkit-scrollbar-button { display: none }`
    자리        **늘 비워 둔다** — `html { scrollbar-gutter: stable }`

**6px 인 이유 — 재권님은 화면을 보고 말씀하셨다.** 설정값과 보이는 폭이 다르다.

    설정 10px  →  실제 **12px**      전에 쓰던 값
    설정  6px  →  실제 **8px**       12 의 2/3

「설정값 10 의 2/3」 으로 읽어 7px 로 잡았다가 **8px 을 못 맞췄다.**
**보이는 폭으로 재야 한다** (2026-09-18 · 홈페이지_정리 실측).

**자리를 비워 두는 이유** — `.kh-app` 이 가운데 정렬이라 세로 스크롤바가
생기면 **화면 전체가 7.5px 왼쪽으로 밀린다** (실측 x=60 → 52.5).
`scrollbar-gutter: stable` 이면 막대가 없어도 자리를 남겨 안 밀린다.

**정하지 않으면 브라우저 기본이 나온다.** 2026-09-18 에 `holdings/` 안
스크롤 칸 **17군데 중 9군데**가 아무것도 안 정해 놓아, 재권님이 종목 뉴스 칸에서
**화살표 달린 두꺼운 윈도우 기본 막대**를 보셨다. 정해 둔 8군데도 값이 갈렸다.

**「감추는 것」 은 덮어쓰기가 아니다 (2026-09-18 지시 — 「감춰둬」).**

    .kh-cat        첫 화면 갈래 띠        홈 · 관심 · 국내 · 미국 …
    .kh-foot       아래 시세 띠           투자 유의사항 · 지수 · 달러 인덱스
    .h-nav         분석·로드맵 가로 메뉴
    .kh-sc-names   업종 이름 줄

**넷 다 「가로로 미는 띠」다.** 막대가 없어도 **글자가 잘려 보이면 더 있다는
것을 알 수 있다.** 재권님께 화면으로 보여드리고 「감춰둬」 로 정하셨다.

**지우지 말 것.** 「하나로만 쓴다며?」 하고 되살리면 띠마다 가로 막대가 생긴다.

**무엇이 검사하나** — **아직 없다.** `base.css` 밖에서 `::-webkit-scrollbar`
에 `width` 나 `background` 를 주는 곳을 찾으면 잡힌다. 감추는 것
(`display:none` · `height:0`)은 빼고 세야 한다.

### 선 — 흰 카드에는 넣지 않는다 (2026-09-15 지시)

- **흰 카드에는 테두리·윗선을 넣지 않는다.** 회색 바탕과 흰 면의 대비로 이미 나뉜다
- **선이 꼭 필요한 자리(표 구분선 등)는 구조용 회색 하나로 통일한다** — `--*-border`
- **선에 상태 색을 쓰지 않는다.** 초록 윗선·빨간 윗선으로 상태를 알리지 않는다

상태는 **라벨 글자로 읽는다.** 「통과」 「불일치」 라고 적혀 있으면 그것으로 충분하고,
색을 얹으면 같은 정보를 두 번 말하면서 화면만 시끄러워진다.

    /* 하지 않는다 — 흰 카드에 상태 색 윗선 */
    .stat { background: var(--pj-bg-card); border-top: 1px solid transparent; }
    .stat.pass { border-top-color: var(--pj-done); }

    /* 이렇게 — 선 없이 흰 카드. 상태는 라벨이 알린다 */
    .stat { background: var(--pj-bg-card); }

**경위** — 흰 바탕 숫자를 검정으로 바꾸면서 색을 윗선으로 옮겼는데, 세 보드에
초록·주황·빨강 윗선이 생겨 오히려 어수선해졌다. 면 구조는 「회색 바탕 + 흰 카드」로
나누기로 이미 정해 두었으므로 선을 도로 뺀다.

**예외를 두지 않는다 (2026-09-15 지시).** 통계·요약 칸의 큰 숫자도 흰 바탕이면
검정이다. 색은 테두리로 남긴다. 예외를 만들면 "여기는 왜 색이지" 를 계속 묻게 되고,
한 화면 안에서 어떤 칸은 검정이고 어떤 칸은 색인 상태로 돌아간다. 실제로 그랬다 —
Debugging 보드의 통계 세 칸에서 「통과」만 검정이고 「변경」·「불일치」는 색이었다.

네 구역 모두 같은 구조의 토큰을 갖고 있다 (`--kh-` · `--cs-` · `--pj-` · 접두사 없음).
구역이 달라도 같은 규칙으로 쓴다.

### 아직 안 정해진 자리는 빨간 바탕으로 남긴다 (2026-09-15 지시)

룰이 없어서 **판단으로 둔 자리**, 재권님이 나중에 보셔야 하는 자리는
**화면에 빨간 바탕으로 표시한다.**

    background: var(--pj-danger-faint);   /* 연한 빨강 */
    color:      var(--pj-danger);         /* 진한 빨강 — 위 색 규칙 그대로 */

**왜** — 문서에만 적어두면 안 보인다. 「나중에 정할 것」을 MD 에 적어두면 아무도
다시 안 읽고, 그대로 굳어 룰이 된다. 화면에 빨간 자국이 남아 있으면 볼 때마다
걸리고, 그래서 정해진다.

- 정해지면 **표시를 지운다.** 빨간 자국이 남아 있다는 건 아직 안 정했다는 뜻이다
- 왜 빨간지 **주석에 적는다.** "무엇을 정해야 하는지" 가 없으면 표시만 남는다
- 급한 오류 표시와 헷갈리지 않게, 이 자리에는 `<!-- 미정: … -->` 주석을 같이 단다

**첫 사례** — `projects/index.html` 의 참고 링크(`.lnk a`). 흰 바탕에 파란 글씨라
위 색 규칙과 어긋나는데, 링크의 파랑은 상태 표시가 아니라 "누를 수 있다" 는
웹 전체의 관례다. 검정으로 바꾸면 밑줄도 없어 링크인지 알 수 없게 된다.
예외로 둘지 규칙을 따를지 재권님이 정하실 때까지 빨간 바탕으로 둔다.

### 하드코딩 금지의 예외 — 「테마 색」과 「데이터로서의 색」 (2026-09-15 정리)

이 규칙은 **테마 색**에만 적용된다. 테마를 바꾸면 따라 바뀌어야 하는 색이 테마 색이다.
반면 **대상이 정해 놓은 색**은 데이터다. 테마를 라이트에서 다크로 바꿔도
삼성 파랑은 삼성 파랑이고 태극기 빨강은 태극기 빨강이다.

| 무엇 | 어디 | 왜 예외인가 |
|---|---|---|
| 회사 브랜드 색 | `holdings/js/data/market.js` | 종목코드·회사명 옆의 **회사 속성**이다. 옮기면 theme.css 가 종목 목록이 된다 |
| 국기 SVG | `holdings/index.html` | 국기 **규정색**이다. 디자인 선택이 아니다 |
| 외부 라이브러리 | `holdings/js/vendor/*` | 우리 코드가 아니다. 갱신하면 되돌아간다 |

**개수는 적지 않는다.** 어느 색을 예외로 뒀는지는 `debugging/expected.json` 의
`allow` 한 곳에 있고, 검사기가 세면서 판정한다. 여기 숫자를 적으면 **검사 기준이
바뀔 때마다 낡는다** — 2026-09-18 에 검사기가 `#fff` 와 `rgb()` 까지 보게 넓어지자
셋 중 둘이 한 번에 움직였다. 국기는 **색 가짓수와 파일에 나오는 자리 수가 달라**
숫자 하나로 적을 수도 없다.

**판단 기준 한 줄** — 테마를 바꿨을 때 이 색도 바뀌어야 하면 변수로, 그대로여야 하면 데이터로.

**무엇이 검사하나** — `python debugging/check.py` 가 본다. 보는 범위는
`debugging/expected.json` 에 적힌 구역 전부다. **폴더로 적어 두었으므로
새 파일이 생겨도 저절로 들어온다** — 2026-09-16 에 `daily.css` 가 목록에 없어서
그냥 지나간 일이 있었다. 다만 **구역 자체는 저절로 들어오지 않는다.**
2026-09-18 에 루트 · `ai-work/` · `debugging/` 셋이 통째로 빠져 있던 것을 찾았다.
그래서 여기에 구역 이름을 나열하지 않는다 — 나열하면 구역이 늘 때마다 낡는다.
걸리면 **바꿔 쓸 토큰 이름까지 함께 나온다**(`#3182f6` → `--accent`).
`allow` 에 적어 둔 국기 규정색은 **그 파일에서만** 예외다. 파일별로 적어 두었으므로
**그 파일에 다른 색이 새로 들어오면 걸린다.**

**셀 때는 주석을 뺀다 (2026-09-15 정리).** 색 검사가 주석을 빼고 세므로 기준을
맞춘다. 주석 안의 색은 "예전에는 이 값이었다" 인 경우가 많아 세면 오탐이 된다.
`market.js` 는 주석에만 있는 색이 따로 있어 두 값이 갈린다.

**⚠️ `#8b95a1` 은 색이 아니라 "대표색을 모른다" 는 뜻이다.** 로고를 못 받은
종목에 넣어 둔 회색이다(`market.js` 주석). 몇 종목이 남았는지는 세어 본다.

    grep -c "brand: '#8b95a1'" holdings/js/data/market.js      아직 모르는 종목
    grep "brand: '#" holdings/js/data/market.js | grep -vc "#8b95a1"    색이 들어간 종목

**`#8b95a1` 을 그냥 세지 않는다.** 주석과 코드 안 비교문에도 나와서, 종목이
하나도 안 남아도 `0` 이 되지 않는다. 세는 것은 `brand` 필드다.

> **로고를 받아 실제 브랜드 색을 연결하는 중이다** (2026-09-15 지시).
> 위 첫 명령이 `0` 을 내면 다 끝난 것이고, 그때 이 문단을 지운다.
> (2026-09-15 지시 — "로고관련된거는 내일 다운받아서 연결한다 로 설명해서 박아놓고
> 작업되면 지우는 방향으로")

`market.js` 는 `주식페이지_개발` 영역이다. 색을 채우는 것은 그쪽이 한다.

데이터로 두는 색에는 **왜 예외인지 주석을 단다.** 안 그러면 다음 사람이 위반으로 보고
또 옮기려 한다. 2026-09-15 에 이 논의를 한 번 더 하게 되어 규칙에 적었다.

### 차트 색은 복제를 피할 수 없다 — 대신 대조한다

canvas 는 CSS 를 상속받지 못한다. 그래서 `holdings/js/theme.js` 가 `theme.css` 를
읽어 색 문자열을 넘기는데, CSS 로드 전에 호출될 때를 위해 `FALLBACK` 에 같은 값을
복제해 두었다. **합칠 수 없는 복제다.** 어긋나면 차트만 옛 색으로 남고 화면은
멀쩡해 보여서 눈으로는 못 찾는다.

    python holdings/tools/check-theme-sync.py      맞으면 0, 어긋나면 1 + 어긋난 항목

`theme.css` 의 `--kh-*` 를 고쳤으면 이것을 돌린다. **몇 개가 맞았는지는 그 출력이 적는다.**

### 같은 값은 한 곳에만 둔다 — 색에 한정되지 않는다 (2026-09-15 지시)

위의 색 규칙은 더 큰 규칙의 한 예다. **두 곳 이상에서 쓰는 값은 한 곳에 두고
가져다 쓴다.** 색·주기·한도·종목 목록·API 코드·문구 전부 해당한다.
바꿀 때 **한 곳만 고쳐야 하고, 그것으로 전부 바뀌어야 한다.**

    값을 고치려고 두 파일을 열고 있다면 그 구조가 잘못된 것이다.

**합칠 수 없는 복제라면 — 주석 대신 대조 도구를 만든다.**

"같아야 한다" 는 주석은 지켜지지 않는다. 어긋나도 화면은 멀쩡해 보이고,
숫자만 조용히 갈린다. 눈으로는 못 찾는다. 선례가 `holdings/tools/check-theme-sync.py` 다.

| 복제 | 왜 못 합치나 | 대조 도구 |
|---|---|---|
| `theme.css` ↔ `holdings/js/theme.js` 의 `FALLBACK` | canvas 가 CSS 를 못 읽는다 | `holdings/tools/check-theme-sync.py` |
| 관심종목 목록 — `market.js` · `dart.py` · `kis-worker.js` | 세 언어·세 실행 환경 | `holdings/tools/check-watchlist-sync.py` |
| 주기·한도 — `kis_proxy.py` ↔ `kis-worker.js` | 파이썬과 자바스크립트 | `holdings/tools/check-kis-consts.py` |
| 보드 API 모양 — `kis_proxy.py` ↔ `projects/worker/board-api.js` | 로컬과 배포본, 두 언어 | **아직 없다** (로컬 API 를 만들 때 함께 본다) |

값을 고쳤으면 해당 도구를 돌린다. 전부 한 번에 보려면:

    python tools/check-all.py

**새로 복제를 만들 때는 대조 도구도 같이 만든다.** 주석만 달고 넘어가지 않는다.
**단, 합칠 수 있으면 합치는 것이 먼저다** — 바로 아래 절을 따른다.

### 합칠 수 있으면 대조 도구보다 먼저 합친다 (2026-09-18 지시)

**대조 도구는 못 합칠 때 쓰는 것이다.** 합칠 수 있는데 검사기를 만들면
복제를 그대로 둔 채 감시만 붙이는 셈이다.

**실제로 있었던 일** — `tools/show-window.py` 의 「하지 않는 일」 목록이
`CLAUDE.md` 의 같은 목록과 **두 번 갈렸다** (2026-09-16 셋 vs 넷 ·
2026-09-18 넷 vs 다섯). 첫 번째는 코드에서 룰 하나를 빼면서 도구 주석만
고친 것이었는데, **목록이 셋인 채로도 글이 자연스러워서 검증에서도 안 걸렸다.**

두 번째에 대조 도구를 만들자는 이야기가 나왔고 **안 만들기로 했다.**

| | 기존 대조 도구 넷 | 이 목록 |
|---|---|---|
| 무엇을 맞추나 | **값** — 색·종목코드·숫자 | **문장** |
| 기계가 판정하나 | 같다/다르다가 나온다 | 한쪽을 더 낫게 고쳐 써도 불일치가 뜬다 |

**글을 못 고치게 만드는 검사가 된다.** 오탐을 줄이려면 항목 수나 짧은 키를
박아야 하는데, 그것은 같은 날 「세면 나오는 값은 적지 않는다」 로 **양쪽에서
빼낸 그 숫자**다. 한 바퀴 돌아 제자리다.

**그래서 주석이 목록을 들고 있는 것을 그만뒀다.** 이제 「그 목록은
`CLAUDE.md` 의 이 절에 있다」 한 줄만 있고, 갈릴 것이 없다.

**언제 만드나** — 아래 둘 중 하나면 신호다.

    같은 **종류**의 복제가 하나 더 나온다
    같은 **자리**가 다시 갈린다        ← 한 번은 사고, 두 번은 구조다

**신호가 오면 「합치거나 만든다」.** 「만든다」 가 아니다. 합칠 수 있는지
먼저 보고, 못 합칠 때만 대조 도구로 간다. 조건을 적어 두는 것은, 안 그러면
다음에 「만들까 말까」 를 처음부터 다시 고민하기 때문이다.

### 한글을 출력하는 파이썬 도구에는 이 두 줄을 넣는다 (2026-09-16)

    if hasattr(sys.stdout, "reconfigure"):
        sys.stdout.reconfigure(encoding="utf-8", errors="replace")

윈도우 콘솔은 기본이 `cp949` 라 `—` 같은 글자에서 **`UnicodeEncodeError` 로 죽는다.**
계산은 다 맞고 출력 한 줄에서 죽으므로, 부르는 쪽은 그냥 실패로 읽는다.

**개발 중에는 멀쩡해 보인다.** 환경변수 `PYTHONIOENCODING` 이 걸린 셸에서
돌리면 콘솔이 UTF-8 이라 아무 일도 안 일어난다. 그 변수가 없는 곳 —
**`.bat` 이 부르는 `cmd` 가 정확히 그 자리다** — 에서만 터진다.
2026-09-16 에 `tools/show-window.py` 가 이렇게 나갔고, 다른 세션이 잡았다.

그래서 **시험은 환경변수를 지우고 한다.**

    PowerShell 에서 부른다
    cmd /c "set PYTHONIOENCODING=& chcp 949 > nul & python tools\<도구>.py"

**Git Bash 에서 `cmd /c` 를 그대로 쓰지 않는다 (2026-09-21 · 2026-09-22 원인 확정).**

**`/c` 가 `C:/` 로 바뀐다.** Git Bash 는 `/` 로 시작하는 인자를 윈도우 경로로
번역하는데, `cmd` 의 스위치도 그렇게 본다.

    (부른 것)   cmd /c "echo hello"
    (닿은 것)   cmd.exe C:/ "echo hello"     ← **스위치가 경로가 됐다**

그래서 `cmd` 가 **대화형으로 뜨고 안 끝난다.** 배너만 나오고 명령은 안 돈다.
`C:/` 를 경로로 읽으려 할 때는 재권님 화면에 이렇게 나온다.

    파일 이름, 디렉터리 이름 또는 볼륨 레이블 구문이 잘못되었습니다.

**어제는 「그냥 멈춘다」 로 적었는데 부정확했다.** 멈추는 것이 아니라
**대화형 셸이 떠서 안 끝나는 것**이다 — 2026-09-22 에 재권님이 그 오류를
보시고 물으셔서 갈렸다.

**둘 중 하나로 부른다 (2026-09-22 실측).**

    cmd //c "echo hello"                      슬래시를 둘로
    MSYS_NO_PATHCONV=1 cmd /c "echo hello"    번역을 끈다

**둘 다 `hello` 가 나온다.** 그냥 `cmd /c` 는 배너만 나온다.

같은 날 도구 **대부분**에 이 두 줄이 없어서 한꺼번에 넣었다.
새 도구를 만들 때도 넣는다 — 빠뜨리면 그 도구만 조용히 죽는다.
- 상승/하락 색은 한국식(상승 빨강 #f04452 / 하락 파랑 #3182f6). 토스 팔레트 값
- accent 색상: #3182f6 (토스 블루). 네 구역 모두 같은 값
- 폰트: Noto Sans KR, Roboto
- 카테고리 제목: 거대 이탤릭 타이포
- 작업물 갤러리: 매저너리(비정형) 그리드
- 스타일 기조: 미니멀, 프로페셔널

## 호스팅
- GitHub 저장소: https://github.com/wornjs2003/kjcstudio
- **배포 주소: https://thekjcstudio.com** (Cloudflare Access 로 보호)
  - `https://wornjs2003.github.io/kjcstudio/` 는 301(영구 이동)로 위 주소에 넘어간다.
    실제 주소가 아니다
- 화면(HTML·CSS·JS·`data/*.json`)은 **푸시하면 자동 반영**된다
- **워커는 자동이 아니다.** 대시보드에 손으로 붙여넣어야 한다 —
  「커밋 · 푸시 · 배포 규칙」 참조

## 파일 구조
- `index.html` : 메인 (풀스크린 인트로 + 카테고리 입구 목록). 전용 파일은 `assets/css/home.css`, `assets/js/home.js`
- `category/` : 카테고리 페이지 (3d-modeling, ai-work, web-interactive, branding)
- `about.html`, `services.html`, `contact.html` : 서브 페이지 (상단 ⋯ 더보기 접근)
- `assets/css/theme.css` : **색상 토큰 단일 기준** (구역마다 같은 구조의 theme.css 를 따로 소유)
- `assets/css/`, `assets/js/`, `assets/images/`, `assets/fonts/` : 메인 사이트 전용 리소스
- `data/categories.json` : 카테고리 목록. 메인의 입구 목록과 각 카테고리 페이지 제목·설명이 모두 여기서 나온다
  - `featured` 는 지금 쓰이지 않는다. `category/*.html` 이 모두 `data-category` 를 직접 갖고 있어 폴백으로만 남아 있다
- `data/works.json` : 모든 작업물 데이터 (이곳만 수정하면 갤러리 자동 반영)
- `partials/nav.html`, `partials/footer.html` : 공통 네비/푸터 (fetch로 동적 주입)
- `holdings/` : 독립 프로젝트 구역 (주식 분석, 보유 종목 — 별도 관리)
- `company-setup/` : 회사 설립 체크리스트 (독립 구역, 항목 데이터는 `company-setup/data/checklist.js`)
- `projects/` : 프로젝트 보드 (독립 구역, 칼럼·카테고리 정의는 `projects/data/config.js`, 데이터는 localStorage + JSON 내보내기)
- `ai-work/` : AI 작업 프로세스를 정하는 판 (독립 구역, 단계·도구 데이터는 `ai-work/data/pipeline.json`)
  - 상단 메뉴의 **AI Work** 는 이곳으로 간다. `category/ai-work.html` 은 그대로 두는데,
    작업물이 생기면 쓸 갤러리라서다. 메뉴에는 하나만 걸 수 있어 **지금 볼 것이 있는 쪽**을 골랐다 (2026-09-17)
- `uidata/` : 화면이 쓰는 **그림 파일**. `data/` 와 헷갈리기 쉬운데 성격이 다르다 —
  `data/` 는 JSON 이고 저장소에 담지만, **`uidata/` 는 담지 않는다**
  - `uidata/icons/` 종목 아이콘. `.gitignore` 에 있어 **내려받아도 저장소에 안 올라간다.**
    없으면 `python tools/fetch-stock-icons.py` 로 받는다 (몇 분 걸린다)
  - 받는 방법·옵션은 `uidata/README.md` 에 있다. 여기 다시 적지 않는다

## 상단 메뉴 구성
- 메뉴 줄 — 3D Modeling / AI Work / Debugging / API / Holdings / Setup / Projects / ⋯(더보기)
- ⋯ 안에 **Web · Interactive** / **Branding** / About / Services / Contact
- **값은 `partials/nav.html` 이 기준이다.** 여기 적은 것은 그 순간의 모습이므로,
  어긋나면 `nav.html` 을 보고 이쪽을 고친다 (「세면 나오는 값은 본문에 적지 않는다」)
- Branding 은 2026-09-14 에 메뉴 줄에서 ⋯ 안으로 옮겼고, **그 자리에 API 가 들어왔다**
  - 주소는 **`/api-board/`** 다. `/api/` 가 아니다 — KIS 워커 Route 가
    `thekjcstudio.com/api/*` 라서 `/api/` 에 페이지를 두면 워커의 JSON 404 가 뜰 수 있다.
    와일드카드라 `/api/` 와 `/api/index.html` 도 그 범위에 들어간다.
    Access 뒤라 실증은 못 했고, 위험을 피하는 쪽을 골랐다 (2026-09-14 판단)
  - 메뉴 이름은 `API`, 주소만 `/api-board/` 다. 나중에 `/api/` 로 "정리" 하지 말 것
  - 페이지(`category/branding.html`)와 `data/categories.json` 의 항목은 **그대로 둔다.**
    메뉴에서만 뺐다. 지우면 되돌리기 번거로워 "우선 옮겨놓는" 것으로 정했다 (지시)
  - 그래서 메인 페이지의 카테고리 입구 목록에는 Branding 이 아직 4개 중 하나로 남아 있다.
    API 페이지가 나오면 그때 함께 정리한다
- **메뉴를 고칠 때는 `partials/nav.html` 의 두 곳을 같이 고친다.**
  데스크톱 목록과 모바일 전체화면 목록이 따로 있다. 한 곳만 고치면 폰과 PC 가 달라진다
- Holdings는 `holdings/` 로 외부 이동 느낌 (화살표 아이콘)
- Setup 은 `company-setup/` 으로 외부 이동 느낌 (화살표 아이콘)
- Projects 는 `projects/` 로 외부 이동 느낌 (화살표 아이콘)

## 확장 규칙
- 작업물 추가(권장): 이미지를 `assets/images/works/<카테고리>/` 에 드롭 → 루트 `refresh-works.command` 더블클릭 → `data/works.json` 자동 갱신
- 작업물 추가(수동): `data/works.json` 에 항목 한 개 직접 추가 (스크립트 안 써도 됨)
- 카테고리 추가: `data/categories.json` 에 추가 + `category/` 에 쌍둥이 HTML 1개 복사 + `tools/refresh-works.py` 의 `CATEGORIES` 배열에 추가
- 공통 UI(네비/푸터) 변경: `partials/` 파일 한 곳만 수정

## 이미지 파일명 규칙 (refresh-works.command 용)
- 기본: `elf-warrior.jpg` → 제목 "Elf Warrior", span 자동 (Pillow 있을 때 비율 분석)
- 순서 지정: `01_elf-warrior.jpg` → order=1
- span 강제: `elf-warrior_tall.jpg` / `_wide.jpg` / `_square.jpg` / `_large.jpg`
- 조합: `02_엘프전사_wide.jpg` → order=2, 제목 "엘프전사"(한글 유지), span=wide
- 한글 파일명 가능, 공백 대신 `-` 또는 `_` 사용

## 구역 구조 규칙 (중요)

**한 저장소로 간다. 별도 저장소로 떼어내지 않는다.** (2026-09-14 지시)

그 전에는 holdings 를 언젠가 분리할 것으로 보고, 부모 사이트 자원을 참조하지 못하게
막아두었다. 그래서 구역마다 같은 것을 복제했다. 분리하지 않기로 정했으므로 복제를 끝낸다.

### 공통 자원은 루트에 하나만 둔다

| 무엇 | 어디 | 누가 쓰나 |
|---|---|---|
| 색·모서리·글꼴 | `assets/css/theme.css` | **네 구역 전부** |
| 상단 메뉴·푸터 | `partials/nav.html` · `partials/footer.html` | **네 구역 전부** |
| 메뉴 주입·모바일 메뉴 | `assets/js/main.js` | **네 구역 전부** |

- 구역별 `theme.css`(`holdings/css/` · `company-setup/` · `projects/`)는 **두지 않는다.**
  2026-09-14 에 네 파일을 루트로 합쳤다 (변수 177개, 누락 0)
- 변수 접두사(`--kh-` · `--cs-` · `--pj-`)는 **그대로 둔다.** 어느 구역 것인지 한눈에 보이고,
  이름을 바꾸려면 쓰이는 곳을 전부 고쳐야 한다 — 위 「공유 자원」 절의
  세는 명령을 쓴다. **범위를 안 좁히면 정의부와 문서까지 세어진다**
- 구역 전용 CSS/JS(`holdings/css/frame.css` 등)는 그 구역 안에 그대로 둔다

### 상단 메뉴는 네 구역이 같다

- 어느 구역에 들어가도 메뉴 7개 + `⋯` 가 보인다. 눌러서 서로 오갈 수 있다
- 메뉴를 고칠 때는 `partials/nav.html` **한 곳만** 고친다
- 구역별 자체 nav 는 두지 않는다

### 복제가 남아 있다면

값이 어긋나는 원인이다. 실제로 그랬다 — 문서엔 다크·`#4f7eff`, 코드엔 라이트·`#3a6ae8` 였고,
구역마다 팔레트가 갈려 화면을 오갈 때 이음매가 보였다. 복제를 발견하면 루트로 합친다.

holdings 관련 룰은 `holdings/CLAUDE.md` 참조.

## 실행 커맨드

### 로컬은 8765 하나로 본다 (2026-09-15 지시)

**`holdings/preview` 하나만 띄운다.** 여섯 구역과 모든 API 가 거기서 돈다.

⚠️ **`holdings-preview.bat`(루트) 이 아니다.** 이름이 비슷한데 하는 일이 다르다.

    holdings-preview.bat    python -m http.server 8765    화면만 열린다. API 가 없다
    holdings/preview.bat    kis_proxy.py --port 8765      이것이 맞다

루트 것은 정적 서버라 `/holdings/` 는 200(정상)으로 열리지만 `/api/kis/*` ·
`/api/dart/*` · `/api/news/*` 가 전부 404(그런 주소 없음)가 되어 **시세도 공시도
뉴스도 하나도 안 나온다.** 2026-09-16 에 이 문서가 루트 것을 가리키고 있어서
실제로 그렇게 됐다.

확인은 이렇게 한다.

    curl http://localhost:8765/api/kis/health     →  200 · ok:true

    http://localhost:8765/              메인
                        /holdings/      주식
                        /projects/      프로젝트 보드
                        /company-setup/ 설립 체크리스트
                        /api-board/     API 보드
                        /api/kis/*  /api/dart/*  /api/news/*

**`/debugging/` 은 8093 으로 넘긴다.** Debugging 보드는 그 전용 서버에서 돈다.

    8093 이 켜져 있으면   `302` 로 넘긴다 → `http://localhost:8093/debugging/`
    8093 이 꺼져 있으면   `503` 과 함께 **무엇을 켜야 하는지** 알려준다

**8093 도 반대로 넘긴다** — 거기서 `/holdings/` 를 열면 8765 로 보낸다. 대칭이다.
그래서 **어느 포트로 들어가도 제자리로 간다.** 「로컬은 8765 하나로 본다」 를
기억하지 않아도 되게 하려고 만든 동작이다.

> 2026-09-16 에 이 문단을 **「여기서 안 열린다」 로 잘못 적었다.** 그때 8093 이
> 꺼져 있어서 `503` 만 보고, 코드를 안 본 채 「일부러 막아 둔 것」 으로 읽었다.
> 조건이 둘인데 하나만 재고 단정한 것이다 (`kis_proxy.py:370~406`).

**왜 하나로 모으나** — 서버가 여럿이면 상단 메뉴로 구역을 오갈 때 404 가 쏟아진다.
`holdings/server/kis_proxy.py` 만 API 를 갖고 있는데, 다른 서버들도 저장소 루트를 통째로
내보내므로 **화면은 열리고 API 만 죽는다.**

    8093(Debugging) 에서 Holdings 메뉴 클릭
      → 8093/holdings/ 로 이동. 포트는 안 바뀐다
      → /api/kis/* · /api/dart/* · /api/news/* 전부 404
      → 시세도 공시도 뉴스도 하나도 안 나온다

2026-09-15 에 재권님이 F12 로 찾으셨다. 포트별로 재보니 이랬다.

    포트    /holdings/  /api/kis/*
    8080      200         404
    8091      200         404
    8093      200         404
    8765      200         200      ← 이것만 온전하다

**배포본에는 이 문제가 없다.** 서버가 하나고 `/api/*` 는 워커가 다 받는다.
로컬에서만 나는 문제다.

프로젝트 보드의 `/api/board/*` 는 **로컬에 아예 없다.** 배포본 워커에만 있고,
로컬에서는 `이 브라우저에만 저장됨` 으로 물러선다. 설계가 그렇다.

### 재부팅하면 로컬 서버는 항상 떠 있게 둔다 (2026-09-16 지시)

**매번 손으로 띄우지 않는다.** 루트 `startup.bat` 이 그 일을 하고, Windows
시작프로그램에 `KJC Studio 자동 시작` 이라는 이름으로 등록되어 있다.

- 띄우는 것은 `holdings/server/kis_proxy.py` 다. 위에 적은 대로 루트
  `holdings-preview.bat` 이 아니다
- 이미 떠 있으면 건너뛴다. 여러 번 실행해도 서버가 겹치지 않는다
- 서버 → 브라우저 → 세션 창 순서로 띄운다

### `.bat` 안에는 한글을 쓰지 않는다 (2026-09-16)

`cmd` 는 배치 파일을 **한 줄 실행할 때마다 파일을 다시 열어 바이트 위치로 되짚는다.**
`chcp 65001` 로 코드페이지를 바꾸면 그 바이트 셈이 달라져서, 아래쪽에 한글이 있으면
위치가 밀려 **줄 한복판부터 실행된다.** 2026-09-16 에 실제로 이렇게 나왔다.

    '/*' is not recognized as an internal or external command
    'n' is not recognized as an internal or external command

세션 여는 단계까지 가지도 못하고 끝났다. 전에 "창이 하나 덜 열렸는데" 하신 것도
같은 원인일 가능성이 크다 — `resume-sessions.bat` 의 주석에 세션 이름이 한글로
박혀 있었다.

- `.bat` 본문은 **ASCII 로만 쓴다.** 한글 안내가 필요하면 파이썬 등에 맡기고
  `.bat` 은 그것을 부르기만 한다
- **`chcp` 는 빼지 않는다 (2026-09-21).** 밀림의 원인은 `chcp` 가 아니라
  **「`chcp` 뒤의 한글」** 이다. 본문이 ASCII 면 코드페이지가 바뀌어도
  되짚을 바이트가 안 어긋난다 — 저장소에 **`chcp` 가 있고 한글이 없는 `.bat` 이
  여럿 있고 전부 정상으로 돈다** (`startup.bat` 이 매일 다섯 서버를 띄운다).

  **빼면 오히려 위험하다.** 이 `.bat` 들이 부르는 파이썬은 `reconfigure(utf-8)` 로
  쓰는데, 콘솔이 cp949 면 그 UTF-8 바이트가 깨져 보인다. `chcp 65001` 이 짝을 맞춘다
- **`timeout /t N` 을 쓰지 않는다.** 입력이 리다이렉트된 채 실행되면
  `Input redirection is not supported` 로 즉시 죽는다.
  `ping -n N 127.0.0.1 > nul` 로 대신한다

### 명령 목록

Mac 은 `.command` 더블클릭, Windows 는 같은 이름의 `.bat` 더블클릭. 동작은 동일.

| 용도 | Mac | Windows |
|---|---|---|
| **전체 (포트 8765) — 이것만 쓰면 된다** | `holdings/preview.command` | `holdings/preview.bat` |
| 작업물 목록 갱신 | `refresh-works.command` | `refresh-works.bat` |

아래는 남겨 두지만 **평소에는 쓰지 않는다.** 그 구역만 따로 볼 일이 있을 때만 쓰고,
띄운 채로 메뉴를 눌러 다른 구역에 가지 않는다.

| 용도 | Mac | Windows |
|---|---|---|
| 메인만 (8080) | `studio-preview.command` | `studio-preview.bat` |
| 설립 체크리스트만 (8090) | `company-setup/preview.command` | `company-setup/preview.bat` |
| 프로젝트 보드만 (8091) | `projects/preview.command` | `projects/preview.bat` |
| Debugging 보드만 (8093) | — | `debugging/debugging.bat` |
| ~~홀딩스 정적 서버 (8765, API 없음)~~ | ~~`holdings-preview.command`~~ | ~~`holdings-preview.bat`~~ |

마지막 줄은 **쓰지 않는다.** 위 「전체」 와 포트가 같아서 먼저 띄운 쪽이 이기는데,
이쪽이 이기면 화면만 열리고 API 가 전부 죽는다. 이름이 비슷해 헷갈리기 쉽다.

- 브라우저: Chrome 우선, 없으면 기본 브라우저
- 두 OS 모두 Python 3 필요 (Windows: Python 3.13 + Pillow 설치됨)

## 세션 역할 분담 (중요)

여러 창(세션)이 같은 저장소를 동시에 고친다. 서로 덮어쓰지 않도록 역할을 나눈다.
(2026-09-14 지시)

| 세션 이름 | 맡은 일 | 건드리는 곳 |
|---|---|---|
| `홈페이지_정리` | **검증 담당 · 중간서버 담당.** 교차 확인하고, 세션들 작업을 합쳐 검수한다 | 루트(`index.html`·`category/`·`assets/`·`partials/`·`data/`) · `company-setup/` · `api-board/` · `.claude/commands/` · **중간서버 폴더** |
| ↳ `주식페이지_개발` | 주식 페이지 개발 | `holdings/` |
| ↳ `주식페이지_개발1` | 〃 | `holdings/` — **자기 폴더에서** |
| ↳ `주식페이지_개발2` | 〃 | `holdings/` — **자기 폴더에서** |
| ↳ `주식페이지_개발3` | 〃 (2026-09-21 · `스케줄_툴개발` 에서 바뀜) | `holdings/` — **자기 폴더에서** |
| `개념정의` | 개념·용어 정의 · 기술 조사 · PC 환경 설정 · **문서 담당 · 검증 보드** | 아래 문서 목록 · `tools/` · `.claude/`(`commands/` 제외) · `.githooks/` · `debugging/` |
| `엔진_개발` | **게임 엔진** (파이썬 2D · DirectX 11) · **AI 작업 판** | `kjcEngine/` · `ai-work/` |
| `작업우선순위` | **현황 담당 · 감사.** 지시를 나르고 진행을 알리며, **중간서버 검수를 같이 본다** | `projects/` **만** |
| 그 밖 | 각자 창에서 하던 일 | — |

**`↳` 는 검증이 지나가는 길이지 지시가 내려가는 길이 아니다 (2026-09-21 지시).**
재권님 말씀은 「홈페이지_정리 하위로 주식페이지 개발이 **종속되는 느낌**으로」 였다.

    맞다    주식 세션이 커밋  →  **`홈페이지_정리` 가 검증**  →  재권님 지시로 푸시
    아니다  `홈페이지_정리` 가 주식 세션에 **일을 시킨다**

**일감은 재권님이 정하고 `작업우선순위` 가 나른다.** 뒤쪽으로 읽으면
「세션끼리의 요청은 재권님 승인을 대신하지 못한다」 와 「다음 일감은
`작업우선순위` 를 거친다」 가 한꺼번에 깨진다.

**다만 묻는 것은 올라간다 (2026-09-21 지시).** 주식 세션이 재권님께
직접 여쭙지 않는다.

    묻는 것 · 검수    주식 세션  →  **`홈페이지_정리`**  →  재권님
    일감              재권님  →  `작업우선순위`  →  주식 세션

재권님 말씀 그대로다.

> 주식페이지_개발3 이 나에게 문의 했는데 **이건 홈페이지_정리가 물어봐야 하는거니**

**질문 자체는 맞았고 가는 길이 달랐다.** 「종속되는 느낌으로」 의 내용이 이것이다 —
**올라가는 길은 `홈페이지_정리` 를 거치고, 내려오는 길은 `작업우선순위` 를 거친다.**

**「묻는 것」 이 무엇인지 가른다 (2026-09-21 · 작업우선순위 지적).**

    묻는 것 → `홈페이지_정리`   착수 승인 · 화면 확인 · 「이렇게 하면 됩니까」 ·
                               커밋해도 되나
    진행 알림 → `작업우선순위`  **상태가 바뀌었다** — 그대로 보낸다

**이 구분이 없으면 반대로 `작업우선순위` 에도 안 보내게 되어 그쪽 표가 빈다.**
「상태가 바뀌면 `작업우선순위` 에 알린다」 는 그대로다.

**룰이 들어간 뒤에도 안 지켜졌다.** `6b0e79f` 가 들어간 그 시각에 이미 옛 길로
올라간 것이 셋 있었고, 재권님께는 **여전히 직접 온 것처럼 보였다.**
재권님이 「이 룰이 적용이 안 되고 있는 거 같아」 라고 하셨다.

**룰을 넣는 것과 그 순간 걸려 있는 것을 돌리는 것은 다른 일이다.**
앞은 문서, 뒤는 사람이 한다. 넣을 때 **그 시각에 진행 중인 것이 있는지**를 본다.

### 주식 작업은 두 세션이 검수한다 (2026-09-21 지시)

재권님 말씀 그대로다.

> 홈페이지_정리 가 주식페이지_개발,1,2,3 의 업무가 되고 커밋하면
> 검수하고 문제가 없는지 **개념정의 셔션이 같이 검수한다.**
> 완료되면 나에게 확인받는다.

    주식 세션 넷이 커밋
      →  **`홈페이지_정리`** 가 합치고 검수한다
      →  **`개념정의`** 가 본다
      →  둘 다 통과하면 **재권님께 확인받는다**

**순서가 있다. 동시에 보지 않는다 (2026-09-21 · 홈페이지_정리 판단).**
검수 요청은 `홈페이지_정리` 에게만 가고, 그쪽이 합친 뒤 `개념정의` 에 넘긴다.

**합쳐지기 전은 그 세션 브랜치일 뿐이다.** 네 세션이 같은 `holdings/` 를 만지므로
**룰을 어겼는지는 합쳐진 결과에서 봐야 한다.** 그것이 중간서버를 둔 이유다.
그리고 세션이 둘에게 따로 보내면 **방금 정한 「묻는 것은 `홈페이지_정리` 를
거친다」 가 그 자리에서 깨진다.**

**화면 링크를 함께 낸다 (2026-09-21 지시).** 재권님이 **「화면링크가 필요해」**
라고 하셨다. 검수 결과를 글로만 적으면 **재권님이 직접 눌러 보실 수가 없다.**

    http://localhost:8767/holdings/daily.html      ← 중간서버(합쳐진 것)
    http://localhost:8765/holdings/daily.html      ← 메인(비교용)

**어느 포트인지 함께 적는다.** 세션마다 포트가 다르고, **창 제목에는 포트가
안 나온다** — 링크가 없으면 어느 화면을 보고 계신지 알 수 없다.

**검수하는 쪽이 틀리면 멀쩡한 작업이 되돌아간다 (2026-09-21).**
그날 `홈페이지_정리` 가 구분선 끌기를 검수하며 두 번 헛짚었다 —
`MouseEvent` 로 보내 「안 끌린다」, 더블클릭에 좌표를 안 줘 「안 먹는다」.
**코드는 `pointerdown` 을 듣고 `e.clientY` 로 손잡이 자리를 본다.**

**만든 쪽 보고를 안 믿고 자기 측정만 봤으면 두 번 돌려보냈을 자리다.**
어긋나면 **만든 쪽에 먼저 묻는다** — 그쪽이 그 코드를 안다.

**넘길 때 「못 본 것」 을 함께 적는다.** 2026-09-21 에 그 줄이 **두 번** 결함을
가리켰다 — 「실제 창에서 못 재봤다」 가 스크롤바를 숨기고 재던 것을,
「시간으로 기다린다」 가 **덜 그려진 화면이 기준으로 박혀 있던 것**을 잡아냈다.
**확인 못 한 자리를 적는 것이 다음 사람이 볼 곳을 가리킨다.**

**보는 것이 다르다. 「그 커밋 안」 과 「저장소 전체」 로 가른다.**

| 세션 | 무엇을 보나 |
|---|---|
| `홈페이지_정리` | **그 커밋이 스스로 맞는가** — 짝·동작·화면·키. 문서를 함께 고쳤으면 그 둘이 맞나 |
| `개념정의` | **그 커밋이 저장소 룰을 어겼는가** — 문서를 안 건드린 커밋이라도 어딘가 적힌 룰에 걸리는가 |

**`작업우선순위` 는 검수에 들어가지 않는다 (2026-09-21 지시).**
재권님 말씀 그대로다 — **「검수는 아니고 업무진행 확인하는거지」.**

    검수         홈페이지_정리 · 개념정의       **무엇이 맞는가**
    진행 확인    작업우선순위                   **어디까지 왔나**

앞은 **기준**이고 뒤는 **시간**이다. 겹치지 않는다.

**처음에 「맞게 됐나 / 룰과 어긋나지 않나」 로 갈랐다가 고쳤다.**
`/crosscheck` 2번이 이미 「문서와 코드가 일치하는가」 라서 **겹쳤다** —
**그것이 바로 「같은 범위를 두 번 보면 확인이 아니다」 다** (2026-09-21 ·
홈페이지_정리 지적). **`/crosscheck` 2번은 「그 커밋이 문서도 함께 고쳤을 때」**
를 보고, **문서를 안 건드린 커밋은 그 항목에 안 걸린다.** 그 바깥이 `개념정의` 몫이다.

**왜 문서 담당이 그 자리인가** — **문서를 쓰는 쪽만 그날 무엇이 들어왔는지 안다.**
2026-09-21 에 `CLAUDE.md` 계열 커밋이 **열네 건**이었다. 검수하는 쪽이 그 사이에
세션을 새로 시작했으면 **그날 들어온 룰을 모르는 채 검수한다.**

**둘이면 서로 미루지 않나** — **같은 일을 둘이 맡으면 미루지만, 보는 것이 다르면
미룰 수 없다.** 남이 안 보는 자리이기 때문이다. 「전파를 둘이 맡으면 서로
「저쪽이 했겠지」 가 된다」 는 **같은 일**을 둘이 맡은 이야기다.

**다만 「각자 무엇을 보는지」 가 위 표에 없으면 그 순간 같은 일이 된다.**
그래서 표를 먼저 적고 흐름을 적는다.

**2026-09-21 에 레인이 크게 움직였다.** `스케줄_툴개발` 이 `주식페이지_개발3` 이
되면서 **업무가 `holdings/` 로 바뀌어**, 들고 있던 넷(`projects/` · `ai-work/` ·
`debugging/` · `api-board/`)이 주인을 잃고 넷으로 나뉘었다.

**한 세션이 두 카테고리를 맡으면 커밋을 나눈다.** `.githooks/pre-commit` 이
한 커밋에 레인이 둘 이상 섞이면 막는다. 지금 `엔진_개발`(`kjcEngine/` · `ai-work/`)과
`홈페이지_정리`(루트 · `company-setup/` · `api-board/`)가 그렇다.
**훅은 세션 이름이 아니라 레인 이름으로 센다** — 안 그러면 주식 세션 넷이
`holdings/` 하나를 함께 쓰는 것이 「넷이 섞였다」 로 잡힌다.

**`엔진_개발` 은 `kjcEngine/` 만 고친다 (2026-09-18 지시).**

> 너는 엔진폴더 안에있는 파일만 수정하고 올릴수 있어

**「올리는 것」은 폴더로 나눌 수 없다.** `git push` 는 이력을 따라 올리므로
**자기 커밋 아래에 있는 남의 것은 함께 간다.**

**다만 위에 쌓인 것은 뺄 수 있다 (2026-09-21 실증).** 커밋을 지정하면
**그 지점까지만** 올라간다.

    git push origin <내 커밋>:main

    쌓인 것        남의 것(위) → 내 것 → 공통
    원격이 받은 것  **내 것 → 공통**      위의 남의 것은 안 갔다

    아래에 깔린 것   **뺄 수 없다**
    위에 쌓인 것     **뺄 수 있다**

임시 저장소로 재봤다. 「폴더로 나눌 수 없다」 는 **아래쪽에만 참이다.**

#### 레인마다 창구가 하나 있다 — 예외가 아니다 (2026-09-21 정정)

재권님 말씀 그대로다.

> 엔진개발은 **예외가 아니고** 엔진개발은 **홈페이지_정리 세션같은곳**이야
> 다만 업무가 달라서 엔진개발에 **혼자 있기 때문**이야

**레인마다 창구가 하나 있다.** 그 레인 것을 합치고 검수하고 올린다.

    주식 레인   `홈페이지_정리`      아래에 넷
    엔진 레인   `엔진_개발`          **혼자라 자기가 겸한다**

**창구가 아닌 세션은 자기 커밋을 직접 올리지 않는다.** `개념정의` 도 그렇다 —
2026-09-21 에 여러 번 직접 올렸는데, **재권님 지시는 있었지만 자리가 아니었다.**
재권님이 「여기서 푸시해도 되는건가? 룰이?」 하고 물으셔서 드러났다.

**「다른 세션에 지정하기 전까지」 는 허가가 사라진다는 뜻이 아니다.**
엔진 레인에 세션이 늘면 **그때 그쪽이 창구가 되고 새 세션이 아래로 붙는다.**

**그래서 조건이 붙는다 (2026-09-18 지시).**

> 이세션은 내가 다른세션에 지정하기 전까지 니가 직접해
> **다른 폴더꺼를 같이 올리거나 하는일이 없이 엔진 폴더만 건드린다면** 직접올리면 될거같아

    올린다      쌓인 커밋이 **전부 `kjcEngine/` 안**일 때
    안 올린다   남의 커밋이 아래 깔려 있을 때 — **재권님께 알린다**

**푸시 전에 이것을 돌린다.**

    git fetch -q
    git log --oneline @{u}..HEAD | wc -l                                올릴 것이 몇 건인가
    git log --oneline @{u}..HEAD -- . ':(exclude)kjcEngine/' | wc -l    그중 남의 것

      앞이 0        **올릴 것이 없다** — 「통과」 가 아니다
      앞이 1 이상 · 뒤가 0   올려도 된다
      뒤가 1 이상   남의 것이 섞였다

**`git diff` 를 쓰지 않는다 (2026-09-18 실측).** `diff` 는 **최종 상태만** 본다.
남의 커밋이 파일을 고치고 그 뒤 커밋이 되돌리면 **diff 에 안 나온다.**
실제로 만들어 재보니 **diff 는 비었는데 남의 커밋이 둘 깔려 있었다.**

**두 값을 따로 내는 이유** — `diff` 하나만 보면 **「올릴 것이 없다」 와
「올려도 된다」 가 똑같이 빈 출력**이다. 못 센 것이 「없다」 로 둔갑한다.

**`git fetch` 를 먼저 한다.** `@{u}` 는 **마지막으로 받아온 시점**의 원격을
가리켜서, 안 하면 낡은 것을 본다. 2026-09-18 에 그것으로 두 번 어긋났다 —
남의 커밋을 모르고 올렸고, 이미 올라간 것을 안 올라간 줄 알았다.

**레인만 보는 것으로는 모자란다 (2026-09-18 · 작업우선순위 지적).**

    자기 레인만인가        **기계가 본다**     위 명령으로 걸린다
    올려도 되는 것인가      **기계가 못 본다**   검증을 거쳤는가 · 막아둔 것인가

같은 날 낮 사고가 뒤쪽이었다 — 올라간 커밋이 **검증 중이라 일부러 막아둔 것**이었다.
레인이 맞아도 **「올려도 되는지」 는 따로 확인해야 한다.**

**「다른 세션에 지정하기 전까지」 라는 단서가 붙어 있다.** 재권님이 푸시를
다른 세션에 넘기시면 이 조건은 없어진다.

2026-09-18 에 실제로 그 자리에서 멈췄다 — 엔진 커밋 하나를 올리려는데
아래에 `주식페이지_개발` 커밋 둘이 깔려 있었다. **되돌릴 것은 없었다.**
셋 다 자기 레인 파일만 건드렸고 이력도 일직선이었다.

`개념정의` 세션은 **묻는 것에 답만 한다.** 조사와 설명·샘플 제공까지가 범위이고,
코드 반영은 해당 레인 세션에 넘긴다. 묻지 않은 작업을 먼저 벌이지 않는다. (2026-09-14 지시)

**단, 문서(`.md`)는 직접 고친다 (2026-09-16 지시).** 전에는 문안만 만들어
`홈페이지_정리` 에 넘겼는데, 한 번 더 손을 타면서 옮겨 적는 실수가 생겼다.
실제로 있었던 일 — 경로를 `각 worker/README.md` 로 뭉뚱그려 넘겼더니 그대로 들어갔고,
`debugging/check.py` 가 「문서에만 있고 저장소에 없음」 으로 잡아냈다.

| 문서 | 무엇 |
|---|---|
| `CLAUDE.md` | 이 파일 |
| `holdings/CLAUDE.md` | 주식 구역 룰 |
| `holdings/worker/README.md` · `projects/worker/README.md` | 워커 배포 절차 |
| `company-setup/README.md` | 설립 체크리스트 안내 |

**`.claude/commands/crosscheck.md` 는 예외로 `홈페이지_정리` 가 든다 (2026-09-16 지시).**
그 파일은 룰이라기보다 **검증할 때 따라가는 그쪽의 작업 절차서**다. 검증하다 구멍을
찾으면 그 자리에서 항목을 더하게 되는데, 문서 레인을 거치면 손이 한 번 더 간다.
**쓰는 쪽이 고치는 것이 맞다.**

- **룰 전파는 `개념정의` 가 한다 (2026-09-17 지시).** 문서를 고치는 쪽이 무엇이
  바뀌었는지 가장 잘 안다. **둘이 맡으면 서로 「저쪽이 했겠지」 가 된다** —
  `작업우선순위` 도 같은 일을 하겠다고 나선 적이 있어 여기 못박는다
- **룰 문서를 고쳤으면 돌고 있는 세션에 알린다 (2026-09-17 지시).**
  `CLAUDE.md` 는 세션이 **시작할 때** 읽힌다. 이미 돌고 있는 세션은 바뀐 것을
  모르고 **아침에 읽은 문서대로 계속 일한다.**

      알릴 것   무엇이 바뀌었는지 · 그 세션에 해당하는지
      받는 쪽   그 부분을 **다시 읽는다.** 「알겠다」 로 끝내지 않는다

  **자기 폴더를 가진 세션에는 「`staging` 에서 받으십시오」 를 함께 말한다
  (2026-09-21).** 메시지는 바로 가는데 **문서는 `main` → `staging` → 폴더**
  를 거쳐야 온다. 말로는 알았지만 **그 폴더의 `CLAUDE.md` 는 아직 옛것**이고,
  세션이 다시 시작하면 그 옛것을 읽는다.

  같은 날 실제로 그랬다 — 룰을 전해 받은 세션이 `git show` 로 커밋을 직접
  읽어야 했다. **전파와 동기화가 다른 길로 간다는 것**을 잊으면 이렇게 된다.

  **받았는지는 두 곳을 센다.**

      git log --oneline staging..main | wc -l    문서가 `staging` 까지 왔나
      git log --oneline HEAD..staging  | wc -l    내 폴더까지 왔나

  **앞 줄이 핵심이다.** 뒤 줄만 보면 `0` 이 나오는데, 그것은 「최신이다」 가
  아니라 **「`staging` 에 있는 것까지는 받았다」** 일 뿐이다. 같은 날 한 세션이
  **「받을 것 0건」 을 보고도 폴더에 그 문서가 없었다.**
  「`0` 이 나오면 범위를 먼저 의심한다」 그대로다.

  **`main` 이라고 할 때 어느 쪽인지 밝힌다.** 로컬 `main` 과 `origin/main` 은
  갈려 있을 수 있다. 폴더를 공유하므로 **커밋하는 순간 로컬 `main` 이 바뀌고**,
  다른 폴더들은 그것을 본다. **푸시는 원격에만 닿는다.**

      세션이 받는 길   로컬 `main` → `staging` → 폴더   **푸시와 무관하다**
      푸시             GitHub · 다른 PC · 백업

  2026-09-21 에 내가 「아직 `main` 에도 안 올라갔습니다」 라고 말했는데
  **원격을 뜻한 것**이었고, 받는 쪽은 로컬로 읽었다. **그때 이미 그 세션
  폴더에 들어가 있었다.** 받는 쪽이 재서 알려 줬다.

  **이 문서를 셀 때는 백틱을 조심한다.** 낱말을 백틱으로 감싸는 일이 매우 흔해서
  (2026-09-21 에 육백 곳 넘게), **검색어에 백틱이 빠지면 `0` 이 나온다.**
  같은 날 한 세션이 방금 들어온 문장을 찾다가 그렇게 `0` 을 봤다.
  **「없다」 가 아니라 「그 표기로는 없다」 였다.**

  `.githooks/post-commit` 이 커밋 직후에 **알려 준다.** 커밋을 막지는 않는다 —
  전파할지는 사람이 판단할 일이다.

  **브랜치가 `main` 이면 「`staging` 에도 내려보내야 합니다」 까지 띄운다
  (2026-09-21).** 「`staging` 에서 받으십시오」 라고 말해도 **`staging` 자체가
  뒤처져 있으면 받을 것이 없다.** 그 구간이 비어 있었다 — 「`staging` 도 두 번
  받는다」 는 **「작업 시작 전 · 올리기 직전」** 인데, 문서 레인이 `main` 에 직접
  올리는 동안에는 **그 두 시점이 안 온다.** 같은 날 네 번 났고, 세 세션이
  옛 문서를 들고 있었다.

  **`main` 일 때만 띄운다.** 이 훅은 어느 워크트리에서든 도는데,
  `kjc-staging` 이나 세션 폴더에서 커밋할 때도 뜨면 헛경보가 된다.

  **`staging` 에 내리는 것은 푸시를 기다리지 않는다 (2026-09-21).**
  `staging` 은 **로컬 `main`** 에서 받으므로 **커밋 직후 바로 내릴 수 있다.**
  훅이 푸시가 아니라 **커밋** 뒤에 띄우는 것도 그래서다.

  같은 날 한 세션이 **「고쳤다는 룰이 내 폴더에 없다」 를 세 번** 알렸다.
  셋 다 **푸시 지시를 기다리는 동안**이었다. **룰은 커밋된 순간 확정인데
  세션은 그것을 못 본다.** 푸시까지 묶어 두면 그 사이가 통째로 빈다.

  **하필 그 세션이 지금 쓰는 룰일 때가 가장 나쁘다.** 그날 실제로 그랬다 —
  옛 명령으로 세었으면 방금 고친 것이 **그 세션에게는 없는 일**이 됐다. 2026-09-17 에 `CLAUDE.md` 가 **하루에 열 번 넘게**
  바뀌었고, 하위 작업자 룰을 넣었을 때 네 세션이 다 몰랐다.

  **레인이 바뀐 것은 그 세션에 반드시 알린다.** 모르면 남의 자리를 만지거나
  자기 자리를 비워 둔다. 오늘 실제로 둘 다 났다.

- **문서를 고쳤으면 커밋 전에 `python debugging/check.py` 를 돌린다.**
  **불일치 0** 이어야 한다. 경로를 잘못 적으면 여기서 걸린다.
  통과 개수는 검사가 늘면 따라 늘므로 **여기 적지 않는다**
  (「검사 도구에 대상 값을 박지 않는다」 — 실제로 2026-09-16 에 「통과 10」 이라
  적었다가 같은 날 12 가 되었다)
- **루트 `CLAUDE.md` 의 경로는 저장소 루트 기준으로 적는다.**
  앞의 구역 이름을 빼고 줄여 쓰지 않는다. 그렇게 줄인 경로의 파일은 실제로 없다
- **없는 경로를 「이렇게 쓰지 마라」 는 예시로도 적지 않는다.**
  검사기는 예시인지 아닌지 가리지 못한다. 2026-09-16 에 이 줄을 쓰다가
  예시로 적은 경로가 그대로 불일치로 걸렸다
- `SESSIONS.md` 는 `tools/update-sessions.py` 가 만든다. **직접 고치지 않는다**
- **`holdings/docs/*` 는 `주식페이지_개발` 이 든다 (2026-09-17 지시).**
  룰이 아니라 **API 조사 기록**이라, 실제로 호출해 보는 쪽이 틀린 것을 바로 안다.
  문서 담당이 들면 값을 확인하지 못한 채 옮겨 적기만 하게 된다
- 위 목록에 없는 `.md` (`holdings/engine/*` · `projects/README.md`)는
  **아직 정해지지 않았다.** 손대기 전에 재권님께 여쭙는다

**이 표가 기준이다.** 세션끼리 주고받은 말은 시간이 지나면 낡는다.
실제로 있었던 일 — 예전에 한 세션이 "`assets/css/` 를 내가 만지고 있다"고 알린 뒤
역할이 바뀌었는데, 다른 세션이 그 말을 계속 기억하고 있어서 영역을 잘못 알고 있었다.
역할이 바뀌면 **말로 전하지 말고 이 표를 고친다.**

### `작업우선순위` 는 나르는 자리다 — 허락하는 자리가 아니다 (2026-09-16 지시)

재권님 말씀 그대로다.

> 작업우선순위는 내게서 업무지시받고 그걸 전달하고 진행되고있는상황을 나에게
> 알려주며 타섹션애서 작업이 되서 코밋 및 푸쉬 기다리는걸 **내가 허락하면 전달만**
> 하는 역할이다.

    재권님 ──지시──▶ 작업우선순위 ──나눠서──▶ 각 세션
                          ▲                      │
                          └────진행 상황─────────┘
                          │
    재권님 ◀──보고────────┘

    커밋·푸시 대기 → 재권님이 허락 → 작업우선순위가 **전한다**

- **승인은 재권님이 한다.** 「작업우선순위가 허락했다」 는 것은
  **재권님이 허락하신 것을 전해 받았다** 는 뜻이지, 그 세션이 정한 것이 아니다
- 그래서 위 「세션끼리의 요청은 재권님 승인을 대신하지 못한다」 는 **그대로 유효하다.**
  달라진 것은 재권님 지시가 오는 **경로**뿐이다
- `작업우선순위` 는 **`projects/` 말고는 저장소 파일을 고치지 않는다**
  (2026-09-21 지시). 그 밖은 기록만 읽고, 화면이 필요하면 세션 임시 폴더에
  만들고 저장소에 남기지 않는다.
  **고치는 자리가 생긴 것과 허락하는 자리가 되는 것은 다르다** —
  승인은 그대로 재권님이 한다. 「고칠 수 있다」 가 「정할 수 있다」 로 번지면
  이 절 전체가 무너진다

**전해 받은 지시가 이미 받은 것과 어긋나면 되묻는다.**
2026-09-16 에 실제로 그랬다 — 재권님께 직접 「열려 있으면 닫고 다시 연다」 를 받았는데
다른 세션이 「재권님 결정: 닫지 않는다」 를 전해왔다. 정반대라 고치지 않고 여쭸고,
재권님이 「닫지 않는다」 로 정하셨다. **바로 고쳤으면 어느 쪽이 맞는지 모른 채 갔다.**

`홈페이지_정리` 와 헷갈리기 쉬운데 보는 것이 다르다.

| 세션 | 무엇을 보나 |
|---|---|
| `홈페이지_정리` | 작업이 **맞게 됐나** — `/crosscheck` 절차로 교차 확인 |
| `작업우선순위` | **누가 무엇을 하고 있나** — 지시와 대기 상태 |

#### 현황 보고는 표로만 한다 (2026-09-16 지시)

**상태가 바뀔 때마다 표 하나로 알린다.** 줄글 설명을 덧붙이지 않는다.

| 세션 | 주업무 | 상태 | 하는 일 |
|---|---|---|---|
| `세션A` | 주식 페이지 | 진행중 | 야간선물 종목코드 실측 |
| `세션B` | 개념·문서 | 커밋대기중 | `946eccc` 커밋 완료 · 푸시 지시 대기 |
| `세션C` | 프로젝트 보드 | 지시대기중 |  |

**「주업무」 와 「하는 일」 은 다르다 (2026-09-16 지시).**
주업무는 그 세션이 **늘 맡고 있는 영역**이고, 하는 일은 **지금 하고 있는 것**이다.
앞 칸은 거의 안 바뀌고 뒤 칸만 계속 바뀐다.

**주업무 값은 「세션 역할 분담」 표의 「맡은 일」 에서 가져온다.**
여기에 다시 적지 않는다 — 레인이 바뀌면 그 표만 고치면 따라온다
(「같은 값은 한 곳에만 둔다」).

위 예시가 **실제 세션 이름 대신 `세션A`·`세션B` 를 쓰는 것도 같은 이유다.**
실제 이름과 경로를 적어 두었더니 레인이 바뀔 때마다 낡았다 — 2026-09-17 에
`ai-work/` 가 더해졌는데 이 표만 옛 값으로 남아, **바로 위 줄이 시키는 것을
이 표가 어기고 있었다.**

**상태는 다섯 중 하나다.**

    진행중        그 세션이 지금 일하고 있다. 재권님이 하실 일이 없다
    확인대기중    **세션이 물은 것**에 재권님 답을 기다리며 멈춰 있다
    지시대기중    **물은 것이 없다.** 일을 안 받아 멈춰 있다
    커밋대기중    작업이 끝났고 커밋 지시를 기다린다
    배포중        푸시 뒤 Cloudflare 대시보드 배포가 남아 있다

`확인대기중` 과 `지시대기중` 은 **재권님이 하실 일이 다르다 (2026-09-16 지시).**
앞은 **답을 주셔야** 하고, 뒤는 **일을 주셔야** 한다. 전에는 뒤쪽을 빈 칸으로 뒀는데,
빈 칸으로는 「답을 기다리는 것」 과 「일이 없는 것」 이 갈리지 않았다.

- **받은 일이 없는 세션은 `지시대기중` 이다.** 칸을 비우지 않는다
- 「하는 일」 칸은 **한 줄로 짧게** 적는다
- **예외 한 줄** — 재권님이 바로 움직이셔야 하는 것(배포 확인 · 화면이 깨진 상태)은
  표 아래에 한 줄 붙인다. 그 외에는 표만 올린다
- **커밋이 있으면 「하는 일」 칸에 해시를 적는다 (2026-09-16 지시).**
  `커밋대기중` 이 두 가지를 덮기 때문이다 — **아직 커밋 전**인 것과,
  **커밋은 끝나고 푸시 지시를 기다리는** 것이다. 해시가 있으면 뒤쪽이라는 뜻이다

      | `세션B` | 개념·문서 | 커밋대기중 | 문서 룰 네 건 — **아직 커밋 전** |
      | `세션B` | 개념·문서 | 커밋대기중 | `946eccc` **커밋 완료 · 푸시 지시 대기** |

  2026-09-16 에 `5c30233` 이 뒤쪽 상태로 오래 있었는데 재권님께는
  「푸시 대기 3시간 20분」 으로만 보였다. 그동안 **배포본 `500`(서버가 터짐)이
  그것 때문인 줄 아셨다.** 실제로는 무관했고, `500` 의 원인은 워커를 대시보드에
  안 올린 것이었다. **상태 이름만으로는 구분이 안 됐다.**

**왜** — 세션이 다섯이라 상태가 수시로 바뀐다. 바뀔 때마다 줄글로 쓰면 읽는 데
시간이 더 걸리고, 정작 **「지금 누구를 봐야 하나」 가 묻힌다.**
형태를 고정해 두면 한눈에 훑고 넘어갈 수 있다.

`배포중` 은 **워커를 고쳤을 때만** 나온다. 화면만 고쳤으면 푸시로 끝난다 —
「커밋 · 푸시 · 배포 규칙」 의 표를 따른다.

#### 다음 일감은 `작업우선순위` 를 거친다 (2026-09-16 지시)

**세션은 「다음은 뭘 할까요」 를 재권님께 직접 묻지 않는다.**

    일이 끝남  →  작업우선순위에 "끝났고, 남은 후보는 이것들입니다"
               →  재권님이 한자리에서 고르심
               →  작업우선순위가 그 세션에 전달

- 보낼 때는 **후보마다 한 줄씩** 적는다 — 무엇인지 · 왜 필요한지 ·
  급한지(지금 깨져 있음 / 보통 / 나중에)
- **그대로인 것** — 작업 착수 승인 · 화면 확인 · 커밋·푸시 지시는
  전처럼 재권님께 직접 받는다. 바뀌는 것은 **「다음 일감을 무엇으로 할지」
  묻는 경로 하나**다
- 급히 깨져 있는 것은 예외다. 재권님이 보시는 화면이 망가진 상태면 먼저 알린다

**왜** — 세션이 다섯이라 「다음 뭐 할까요」 가 **다섯 곳에서 따로** 올라왔다.
재권님이 그때마다 문맥을 다시 잡으셔야 했고, **어느 것이 급한지는 한 세션
안에서는 보이지 않는다.** 한자리에 모으면 견주어 고르실 수 있다.

#### 상태가 바뀌면 `작업우선순위` 에 알린다 (2026-09-16 지시)

표를 만드는 것은 `작업우선순위` 인데, **각 세션이 알려주지 않으면 알 수 없다.**

**재권님께 보고할 때 `작업우선순위` 에도 같이 알린다.**

따로 챙길 일이 늘지 않는다. **보고하는 그 시점이 곧 상태가 바뀐 시점**이라
타이밍도 맞는다.

**왜** — 알리지 않으면 그 세션은 **남의 기록을 읽어서 알아내는 수밖에 없다.**
그 방식은 이미 한 번 사고를 냈다. 2026-09-16 에 `작업우선순위` 가 세션 목록에
**`홈페이지_정리` 로 올라갔다.** 남의 기록을 읽는 것이 일이라, 읽은 남의 이름이
자기 기록에 남아서다 (`206ac38` 에서 고쳤다).

**표 형식만 정하고 「누가 언제 알리는지」 가 비어 있으면 표가 낡은 채로 굴러간다.**

#### 셋은 모든 진행사항을 공유받는다 (2026-09-21 지시)

재권님 말씀 그대로다.

> **작업우선순위 개념정의 홈페이지_정리 이 세 세션은 모든 업무의 진행사항을
> 공유받아야 됨. 개념정의도 항상 공유되도록 해줘.**

**받는 것이 요건이고, 보내는 길은 하나로 둔다.**

    세션  →  **`작업우선순위`**  →  `개념정의` · `홈페이지_정리`

**왜 한 곳으로 모으나** — 세 곳에 따로 보내면 **품이 세 배**가 된다. 그날
진행 알림이 세션당 열 번 넘게 나왔으니 **하루 백 번 넘는 발송**이 된다.
그리고 **한 곳도 여러 번 빠졌다.** 세 곳이면 더 빠진다.
**지켜지지 않는 룰은 없는 것과 같다.**

**무엇이 어느 길로 가나 — 「넣는다/뺀다」 가 아니라 「어느 경로」 다**
(2026-09-21 · 홈페이지_정리 지적).

| 경로 | 무엇을 나르나 |
|---|---|
| `작업우선순위` | **어디까지 왔나** — 착수 · 커밋 · 통과/막힘 · 방향 바뀜 · 재권님께 직접 받은 답 |
| 검수 | **무엇이 맞나** — 짝 · 동작 · 화면 · 키 · **못 본 것** |

**섞으면 둘 다 망가진다.** 진행 알림에 검수 상세가 섞이면 「지금 누구를 봐야
하나」 가 묻히고, 검수에서 상세를 빼면 받는 쪽이 볼 것이 없어진다.

**「못 본 것」 은 검수 경로에 둔다.** 그날 그 줄이 **두 번 결함을 가리켰다** —
「실제 창에서 못 재봤다」 가 도구가 스크롤바를 숨기고 재던 것을,
「시간으로만 기다린다」 가 덜 그려진 기준이 박혀 있던 것을 찾아냈다.
**진행 알림에 섞여 갔으면 그 줄이 묻혔을 것이다.**

**약점 둘을 적어 둔다.**

    `작업우선순위` 가 빠지면   **둘 다 빠진다**
    세션이 안 보내면           **비었다는 것을 아무도 모른다**

**뒤쪽이 더 나쁘다.** 「알린다」 는 **기억해야 지켜지는 룰**이고 **빠져도 아무
신호가 없다** — 커밋 훅처럼 걸리는 자리가 없다. **당장 풀 방법은 없다.**
적어 두는 것은 **나중에 훅으로 옮길 자리**를 표시해 두는 것이다 —
같은 날 `staging` 내리기가 그렇게 옮겨졌다.

#### 재권님께 직접 받은 답도 알린다 (2026-09-21 지시)

재권님 말씀 그대로다 — **「직접 확인해도 너에게 알리는 걸 룰로 잡아」.**

앞 절은 **「보고할 때 같이」** 인데, **재권님이 먼저 답을 주시면 그 시점이 안 온다.**
세션은 답을 받고 다음 일로 가고, 현황 목록만 낡는다.

    확인 · 승인 · 반려 · 방향 바꿈  —  **직접 받았으면 그것도 알린다**

**누가 알리나 — 답이 온 길을 따른다.** 겹치지 않는다.

    창구를 거쳐 내려왔다   **창구가** 내려보내면서 `작업우선순위` 에도
    세션이 직접 받았다     **그 세션이** 알린다

**무엇을 받았는지까지 적는다.** 「확인받았습니다」 만으로는 무엇이 닫혔는지 모른다.

    하지 않는다   재권님 확인받았습니다
    이렇게        뉴스·공시 화면 **확인받음**(「오키」) · 커밋 지시까지 받음

**왜** — 2026-09-21 에 재권님이 화면 둘을 보시고 답을 주셨는데 그것이 안 올라와,
`작업우선순위` 가 재권님께 **「아직 안 보셨습니다」** 로 올렸다. 재권님이
**「이거 했는데 공유 안 됐나?」** 하고 물으셔서 드러났다.

**같은 날 길을 바꾸면서 생긴 자리다.** 올라가는 길과 내려오는 길이 갈렸는데,
**답이 돌아오는 길에 `작업우선순위` 가 없었다.**

    올라간다   주식 세션 → `홈페이지_정리` → 재권님
    내려온다   재권님 → 창구 또는 세션 → **`작업우선순위` 에도**

**기계는 이것을 못 본다.** 다만 `작업우선순위` 가 **자기 목록과 실제가 어긋난
횟수**는 셀 수 있다. 안 줄면 다른 방법을 찾는다.

#### 하다가 찾은 것은 `작업우선순위` 에 남긴다 (2026-09-16 지시)

앞 절과 헷갈리기 쉬운데 다르다. **앞은 「내 상태」 를 알리는 것이고,
이것은 「찾은 일감」 을 남기는 것이다.** 둘 다 해야 한다.

일하다 눈에 걸린 것 — **지금 안 고치는 것 · 남의 레인 것 · 나중에 할 것** — 은
반드시 `작업우선순위` 에 남긴다.

- **「한 줄이면 고쳐지는데」 싶은 것도 남긴다.** 목록에서 빠지면 없는 일이 된다
- 해당 세션에 직접 알리는 것은 그대로 한다.
  **알리는 것과 남기는 것은 별개다.** 알리기만 하면 목록에는 안 잡힌다
- 남길 때 세 줄로 적는다 — **무엇 / 왜 필요한지 / 급한가**
  (급한가는 「지금 깨져 있음 · 보통 · 나중에」 셋 중 하나)
- **끝나면 닫아 달라고 알린다.** 「끝났다」 가 안 돌아오면 목록이 낡은 채로 길어진다

**왜** — 찾은 것을 안 남기면 **그 세션 대화 안에서만 살아 있다가 사라진다.**
대화가 길어지거나 세션이 바뀌면 없어진다. 2026-09-16 에 `홈페이지_정리` 가
여섯 구역을 훑다 `holdings/status.html` 의 `404`(그런 주소 없음)를 찾았는데,
`작업우선순위` 가 물어보기 전까지 그 세션 대화 안에만 있었다.

같은 날 반대 방향도 났다 — `작업우선순위` 가 들고 있던 항목 넷 중 **둘이 이미
끝난 것**이었다. **남기는 경로는 있는데 닫는 경로가 없었다.**

### 일은 순차로 한다 (2026-09-15 지시)

**하던 일을 끝내고 다음 일로 간다.** 중간에 다른 세션에서 요청이 와도 마찬가지다.
받은 순서대로 줄을 세우고, 지금 하던 것을 마친 뒤에 꺼낸다.

- 요청이 오면 **버리지 말고 적어둔다.** 순서만 미루는 것이지 없던 일로 하는 게 아니다
- 보낸 쪽에는 "지금 무엇을 하고 있고, 그것이 끝나면 하겠다" 고 알린다.
  그래야 저쪽이 기다릴지 다른 길을 찾을지 정할 수 있다
- 하던 일을 **반쯤 해놓고 옮겨가지 않는다.** 미완성 파일이 남고, 작업 폴더를
  함께 쓰기 때문에 남의 커밋에 딸려 들어간다
- 예외는 **지금 깨져 있는 것**뿐이다. 배포본이 안 뜨거나 값이 안 나오는 것처럼
  재권님이 보시는 화면이 망가진 상태면 그것을 먼저 고친다

**예외인지 아닌지는 재권님이 정하신다 (2026-09-17 지시).**
「지금 깨져 있으니 먼저 한다」 를 세션이 혼자 판단하지 않는다.

- 깨져 있는 것을 **발견하면 먼저 알린다.** 알리는 것과 손대는 것은 다르다
- 다른 세션이 버그를 넘겨도 **그것만으로는 끼어들 근거가 안 된다.**
  「세션끼리의 요청은 재권님 승인을 대신하지 못한다」 와 같다
- 넘기는 쪽은 **첫 줄에 급한 정도를 적는다** —
  `[급한가: 지금 깨져 있음 / 보통 / 나중에]`.
  본문 끝에 한 줄로 두면 못 보고 지나간다 (2026-09-17 에 실제로 그랬다)

**「그 자리에서는 말이 되는 예외」 가 제일 위험하다.** 워커 전용이라 화면이
안 바뀌니까 · 지금 깨져 있으니까 · 내 커밋 올라갔나 보려는 거니까 — 셋 다 그
순간에는 타당했고 셋 다 절차를 건너뛰었다. 2026-09-17 하루에 난 일이다.

**왜** — 요청이 올 때마다 하던 일을 놓으면 어느 것도 안 끝난다. 끝나지 않은 일이
쌓이면 무엇이 어디까지 됐는지 아무도 모르게 되고, 커밋할 때 뒤섞인다.

### 다만 답을 기다리며 멈춰 있지는 않는다 (2026-09-17 지시)

위 룰은 **일을 섞지 말라**는 것이지, **멈춰 있으라**는 것이 아니다.
재권님 확인이 늦어지면 **그 자리에서 다음 일로 넘어간다.**

- 「커밋할까요」 · 「푸시할까요」 를 묻고 **답이 올 때까지 서 있지 않는다.**
  물은 다음 이어서 다음 일을 한다
- 답이 오면 그때 처리하고, 그동안 한 일이 있으면 **둘을 묶어서 함께 여쭙는다.**
  커밋을 합치라는 뜻이 아니다 — **커밋은 따로 하고 묻는 것만 묶는다**
- 재권님이 말씀하신 기준은 **3분**이다. 다만 세션은 답을 기다리는 동안
  돌고 있지 않으므로 **시간을 재지 않는다.** 묻고 나서 멈추지 않으면 같은 뜻이 된다

**넘어가기 전에 무엇이 어느 일인지 적어 둔다.** 작업 폴더가 하나라
두 일의 변경이 한자리에 놓인다. 경로를 지정해 커밋하면 나눌 수 있지만,
**어느 파일이 어느 일인지 기억에 맡기면 섞인다.**

그래서 **앞 일을 커밋까지 끝낸 뒤 넘어가는 편이 안전하다.** 커밋 지시를 아직
못 받았으면, 다음 일로는 **파일을 건드리지 않는 것**(조사 · 확인 · 문서 읽기)을
고른다. 미커밋 둘이 겹치는 상태를 만들지 않는다.

### 일이 여럿이면 하위 작업자에게 나눈다 (2026-09-17 지시)

**서로 기대지 않는 일은 하위 작업자를 여럿 띄워 한꺼번에 돌린다.**
조사·검증만이 아니라 **개발도 포함한다.**

시키는 말은 이런 모양이면 된다.

    "하위 작업자 둘로 나눠서 해줘. worktree 써서.
       하나 — daily.js · daily.css 에 07:30 발송 붙이기
       하나 — chart.js 에 120일선 넣기"

- **각자 맡을 파일을 말할 때 함께 정한다.** 안 정하면 둘이 같은 파일을 연다
- **`worktree` 를 쓴다.** 하위마다 별도 폴더가 생겨 원본을 건드릴 수 없다.
  Claude Code 가 막는다 — 원본 경로를 향한 수정·명령·`git` 우회가 전부 차단된다
- 하위가 **자기 폴더에서 고치고 돌려본다.** 코드만 넘기면 실행을 못 해 버그가 그대로 남는다
- **커밋은 상위가 모아서 한 번에 한다.** 하위에게 커밋·푸시·배포를 맡기지 않는다
- **하위가 다 끝난 뒤 커밋한다.** 하나라도 돌고 있으면 커밋하지 않는다
- **결과는 상위가 합쳐서 한 번에 보고한다.** 하나씩 올리면 읽을 것만 늘어난다

**착수 승인은 나누기 전에 받는다.** 작업자마다 따로 여쭙지 않는다.

#### 하위가 못 하는 것 — 화면 확인

**로컬 서버는 `8765` 하나뿐이고 그것은 원본을 본다.** 하위가 고친 화면은 안 뜬다.

    하위가 할 수 있다   코드 고치기 · 문법·단위 실행 · API 호출 · 조사
    상위가 해야 한다    브라우저로 확인 · 재권님께 보여드리기 · 합치기 · 커밋

그래서 **화면을 눈으로 봐야 끝나는 일은 절반만 하위 몫이다.**
고치는 것은 하위가, 「이렇게 보이는 게 맞습니까」 는 상위가 한다.

#### 쪼갤 값이 없으면 쪼개지 않는다

- **셸 한 줄로 되는 일은 쪼개지 않는다.** `for … & wait` 로 동시에 돌릴 수 있으면 그렇게 한다.
  하위는 토큰이 들고 결과를 다시 합쳐야 하므로, 그 값이 나올 때만 쓴다
- **각각이 몇 초 이상 걸리고 서로 기다리지 않을 때만** 값이 생긴다.
  2026-09-17 실측 — API 세 번이 순서 0.96초 → 동시 0.73초, 화면 두 장이
  10.3초 → 10.0초(헤드리스 크롬이 자원을 다툰다), 검증 한 바퀴가 다 합쳐 2초였다
- 독립적인 것이 **둘 이상일 때만** 쪼갠다. 하나뿐이면 그냥 한다

#### 화면과 정리

**칸으로 나뉘지 않는다.** Windows Terminal 은 분할 창을 지원하지 않는다.
프롬프트 아래에 **목록**이 뜨고, `↑↓` 로 골라 `Enter` 를 누르면 그 하위의 대화가 열린다.
거기서 직접 말을 걸 수도 있다. 끝나면 30초 뒤 목록에서 사라지고, `/tasks` 로 다시 볼 수 있다.

**필요 없어지면 지우기 쉽다.** 폴더는 `.claude/worktrees/` 안에 생기고
그 자리는 이미 `.gitignore` 에 걸려 있다.

    git worktree remove <경로>

아무것도 안 고치고 끝난 하위 폴더는 **저절로 지워진다.** 고친 것이 남아 있으면
지우지 않는다 — 작업이 사라지면 안 되기 때문이다.
**하위가 커밋했으면 브랜치가 남는다.** 합치고 나서 그 브랜치도 지운다.

**`.worktreeinclude`** 에 적은 파일은 새 폴더마다 자동으로 복사된다.
`secrets.json` · `market.db` · 토큰 캐시가 거기 있다 — 없으면 서버가 안 뜨고
차트가 빈 채로 그려져 **하위가 고친 것을 돌려볼 수 없다.**

#### 막혀 있으면 알린다

세션에 따라 **「사용자 · `CLAUDE.md` · 스킬이 요청할 때만 하위를 띄운다」** 는
동작 규칙이 걸려 있다. **이 절이 `CLAUDE.md` 에 들어가면서 그 조건이 충족됐다** —
문서가 요청하고 있으므로 이제 띄울 수 있다 (2026-09-17 확인).

그래도 막히는 세션이 있으면 **그대로 넘어가지 말고 재권님께 알린다.**
혼자 순서대로 처리하면 재권님은 그 세션이 느린 줄로만 아신다.

### 개발 세션이 지킬 것

- **작업을 한 덩어리 끝내면 커밋 전에 `홈페이지_정리` 에게 검증을 요청한다.**
  `SendMessage` 로 무엇을 왜 고쳤는지, 건드린 파일이 무엇인지 보낸다
- 다른 세션이 들고 있는 파일은 건드리지 않는다. `git status` 로 먼저 확인한다
- 커밋할 때는 **경로를 지정해서** 자기 작업분만 담는다. `git commit -a` 를 쓰지 않는다
  (전에 `projects/` 가 남의 커밋에 딸려 들어갈 뻔했다)

### 검증 담당(`홈페이지_정리`)이 할 것

- 검증 절차는 `/crosscheck` 에 적혀 있다. **매번 그 항목을 그대로 따라간다**
- 검증이 끝나면 재권님께 보고하고, 문제가 있으면 해당 세션에도 알린다
- 다른 세션이 작업 중인 것을 알게 되면 `SendMessage` 의 `notify_when_idle` 로
  **작업이 끝나는 시점을 구독한다.** 한 번 받으면 끝나므로, 알림을 받은 뒤 다시 건다
- 검증은 **읽기 위주**로 한다. 남의 작업 파일을 고치지 않고, 문제를 찾으면 알려준다

### 푸시 · 배포 절차

개발 세션은 **커밋까지만 하고 멈춘다.** 올리는 것은 **그 레인의 창구**다
(위 「예외가 아니라 창구다」). 그 뒤는 아래 순서로 간다.

1. 개발 세션: 커밋 → `홈페이지_정리` 에게 알림.
   **워커 파일을 고쳤으면 그 사실을 함께 알린다**
2. `홈페이지_정리`: 검증 → 재권님께 보고.
   워커가 바뀌었으면 **"푸시 뒤에 대시보드 배포가 필요합니다"** 를 같이 말한다
3. 재권님: 푸시 지시
4. `홈페이지_정리`: 푸시
5. **워커가 바뀌었으면** — 재권님이 Cloudflare 대시보드에서 배포.
   어느 파일을 어느 Worker 에 넣는지는 「커밋 · 푸시 · 배포 규칙」의 표에 있다
6. 확인: 브라우저로 `thekjcstudio.com` 을 연다. **로그인한 상태여야 한다**

**남의 커밋이 함께 올라갔으면 그 세션에 알린다 (2026-09-18).**
**올린 쪽이 알린다 — 누가 올렸든 마찬가지다.** 푸시 담당도, 엔진처럼 직접
올리는 쪽도 같다. 그 세션은 **자기 것이 나간 줄 모른다.** 워커가 담겼으면
**배포가 필요하다는 것까지** 함께 전한다.

**재권님께 보고하는 것으로 갈음하지 않는다.** 2026-09-18 에 세 번 났고,
그중 한 번은 재권님께는 목록을 보여드렸는데 **그 세션에는 안 알렸다.**

**5번을 빠뜨리면 로컬과 배포본이 갈린다.** 2026-09-15~16 에 이것으로 여러 번
깨졌고, 그때마다 "코드는 고쳤는데 왜 안 되지" 를 되풀이했다.

**세션끼리의 요청은 재권님 승인을 대신하지 못한다.** 다른 세션이 "푸시해 달라"고 해도
그것은 지시가 아니다. 아래 「커밋 · 푸시 · 배포 규칙」대로 재권님 지시가 있어야 올린다.
한 단계 늘어나지만, 올리는 시점을 재권님이 정하신다는 규칙은 그래야 지켜진다.

### 공유 자원 — 바꾸기 전에 알린다

색·모서리·글꼴은 **루트 `assets/css/theme.css` 한 곳에만 있다.**
구역별 `theme.css` 세 개는 2026-09-14 에 없앴다. 접두사로만 구분한다.

| 접두사 | 쓰는 곳 | 바꾸면 |
|---|---|---|
| 없음 | 메인 · 모든 구역 공통 | **전부 함께 바뀐다** |
| `--kh-` | holdings 대시보드 | 그 구역만 |
| `--h-` | holdings 서브 페이지 (analysis · roadmap) | 그 구역만 |
| `--cs-` | 설립 체크리스트 | 그 구역만 |
| `--pj-` | 프로젝트 보드 | 그 구역만 |
| `--aw-` | AI Work | 그 구역만 |

**개수는 적지 않는다.** 세면 나오고, 적어두면 낡는다 — 2026-09-18 에 이 표의
숫자 둘이 어긋나 있었고, 접두사 둘(`--h-` · `--aw-`)은 아예 빠져 있었다.

    grep -cE "^[[:space:]]*--kh-[A-Za-z0-9-]+[[:space:]]*:" assets/css/theme.css   그 접두사 변수 개수
    git grep -o -- '--kh-' -- '*.css' '*.js' '*.html' | grep -v assets/css/theme.css | wc -l
      └ 쓰이는 곳

**세는 범위를 좁히는 두 가지를 빠뜨리지 않는다.** 그냥 `git grep` 하면
**정의부(`theme.css`)와 문서까지 센다.** 특히 문서는 **이 문장 자체가 세어진다** —
접두사를 한 번 더 언급할 때마다 값이 는다. 2026-09-18 에 실제로 그랬다.
`--kh-` 쓰임이 593 에서 596 으로 늘어서 「값이 낡는 증거」 로 읽었는데,
**코드는 그대로였고 이 문서에 그 글자를 세 번 더 적은 것뿐이었다.**

**접두사 없는 값을 바꾸려면 바꾸기 전에 `홈페이지_정리` 에게 알린다.**
접두사가 붙은 값은 그 구역 세션이 알아서 바꾼다.

**여기서 훅과 룰이 갈린다 (2026-09-22).**

    훅이 보는 것   **파일 경로**    `assets/` → `홈페이지_정리`
    이 룰이 보는 것 **변수 접두사**  `--kh-` → 주식 구역

**`assets/css/theme.css` 한 파일에 여섯 구역 변수가 함께 있어서 경로로는 못
나눈다.** `--kh-` 만 고쳐도 훅은 `홈페이지_정리` 레인으로 세고, 주식 파일과
함께 커밋하면 **「레인이 섞였다」 로 멈춘다.**

**고쳤다 (2026-09-22).** `theme.css` 는 **레인을 정하지 않는다** —
**함께 담긴 파일이 정한다.**

    theme.css + holdings/   →  주식 레인 하나         **통과**
    theme.css 만            →  레인 0                 통과 (섞인 것이 없다)
    theme.css + holdings/ + projects/  →  레인 둘     **막힌다** (맞다)

**처음에 생각한 길은 비쌌다.** 「diff 의 추가·삭제가 전부 한 접두사인지」 를
보려 했는데, **여러 줄 주석 안의 `` `--cs-danger` `` 까지 세어져서** 줄 단위
grep 으로는 안 갈린다. 그 커밋이 `--kh-` 만 고쳤는데 셋으로 잡혔다.

**「값싼지 안 재봤다」 고 적어 두고 재보니 비싼 쪽이었다.** 적어 두지 않았으면
그대로 만들었을 것이다.

**함께 담긴 파일로 가르는 것은 싸다.** 실측으로 그 커밋 여덟이 전부 갈렸다.
우회(`--no-verify`)가 **일곱 번** 난 뒤에 고쳤다 — 「한 번은 사고, 두 번은
구조다」 를 한참 넘겼다.

전에는 파일이 네 개라 "한 곳만 고치면 이음매가 보인다" 가 주의사항이었다.
지금은 반대다. 파일이 하나라 어긋날 일은 없고, **공용 값을 무심코 바꾸면
네 구역이 한꺼번에 바뀌는** 것이 위험이다.

`--kh-*` 를 바꿨으면 `python holdings/tools/check-theme-sync.py` 로 차트 색까지 맞는지 본다.

### 왜 이렇게 하나

세션끼리는 서로를 못 본다. 알려주지 않으면 같은 파일을 동시에 고치거나,
한쪽만 고치고 끝난다. 실제로 있었던 일 — `kis_proxy.py` 는 고쳤는데
`kis-worker.js` 를 빼먹으면 로컬과 배포본의 숫자가 갈린다.

## 세 자리로 나눈다 — 임시서버 구조 (2026-09-18 지시)

재권님 말씀 그대로다.

> **메인서버** — 나는 이것만 최종 확인한다. 여기서 완료된 것만 최종으로 올린다.
> **중간서버** — 세션별로 작업된 걸 머지 및 확인하고 검수한다.
> **각 세션들** — 각자 작업을 한다. **작업 파일이 겹쳐도 동시에 여러 세션이 작업이 가능하다.**

| 자리 | 무엇을 하나 | 누가 |
|---|---|---|
| **메인서버** (`main`) | **재권님이 최종 확인하신다.** 완료된 것만 여기로 올라가고, 여기서 배포된다 | 재권님 |
| **중간서버** (`staging`) | 세션들 작업을 **머지 · 확인 · 검수**한다. 어긋나면 그 세션으로 돌려보낸다 | `홈페이지_정리` · `작업우선순위` 가 감사 |
| **각 세션** (`work*`) | 각자 폴더에서 작업한다. **같은 파일을 동시에 고쳐도 된다** | 개발 세션들 |

### 동기화는 **중간서버**에서 받는다

**`main` 이 아니다.** `main` 은 재권님이 확인하신 데까지고, **다른 세션이 방금 한 것은
`staging` 에 있다.** `main` 에서 받으면 남의 작업을 모른 채 일하게 되고, 나중에
합칠 때 한꺼번에 부딪힌다.

    2026-09-18 실측 — main 에서 받았으면 다른 세션 작업 4건을 놓쳤다

**두 번 받는다.**

    작업 시작 전    git merge staging      최신을 받고 시작한다
    올리기 직전     git merge staging      한 번 더 받는다     ← 이것이 핵심

**뒤쪽이 핵심이다.** 올리기 전에 받으면 충돌이 **내 자리에서** 난다.
내가 방금 고친 코드라 무엇을 살려야 하는지 내가 제일 잘 안다.

안 받고 올리면 충돌이 **중간서버에서** 나고, 합치는 세션이 **남의 코드 두 개를 보고**
골라야 한다. 문법은 맞는데 동작이 어긋나는 형태로 풀려도 화면은 멀쩡해 보인다.

**충돌이 나도 다른 자리는 멀쩡하다.** 2026-09-18 시험에서 일부러 같은 줄을
둘이 고쳤더니, 충돌은 받은 그 폴더 한 곳에만 있었고 나머지는 전부 깨끗했다.
**막히는 것은 한 자리뿐이고 나머지는 계속 간다.**

### `staging` 도 두 번 받는다 (2026-09-21 지시)

**위는 세션 폴더 이야기였다. `staging` 도 같은 것을 한다.**

    staging 에서   작업 시작 전   git merge main
                   올리기 직전    git merge main

**뒤쪽이 핵심이다 — 검수하는 동안 `main` 이 움직인다.**

`main` 을 안 받으면 **검수가 절반만 합쳐진 것을 본다.** 중간서버의 존재 이유가
「합쳐진 것을 본다」 인데 그것이 무너진다. **합쳐지지 않은 상태를 보고 「통과」 를
내는 셈이다.**

**`main` 에 입구가 셋이라서 생긴다.**

    staging      검수를 거친 것
    문서 레인     메인 폴더에서 직접 올린다
    엔진_개발     메인 폴더에서 직접 올린다

**입구를 하나로 줄일 수 없다.** 메인 폴더에서 브랜치를 바꾸면 모든 세션 화면이
동시에 바뀐다(「브랜치로는 못 나눈다」). 재권님이 최종 확인하시는 자리이기도 하다.
**없애지 말고 받는 시점으로 푼다.**

**2026-09-21 에 실제로 났다.** `staging` 이 세션 폴더 작업을 머지해 검수하고
통과시킬 때, `main` 에는 문서 레인 커밋 둘이 이미 있었고 `staging` 은 몰랐다.
**문서라 무사했다. 코드였으면 검수가 헛것이었다.**

**「받으면 따라온다」 를 재보지 않고 말하지 않는다.** 같은 날 내가 새 세션에게
「`merge staging` 하면 그 작업이 따라옵니다」 라고 안내했는데, **그 커밋은
자기 브랜치에만 있었다.** 커밋했다는 말만 듣고 `staging` 에 올라갔는지는
확인하지 않았다. 받는 쪽이 재서 알려 줬다.

    git branch -a --contains <커밋>     어느 가지에 있는지 센다
    git log --oneline <가지>..staging   그 가지가 받을 것이 몇 건인지

**커밋했다는 것과 `staging` 에 있다는 것은 다르다.**

이렇게 하면 머지 커밋은 **`staging` 쪽에서만** 나고 `staging → main` 은 일직선이다.

### 무엇이 어디에 있나

| 자리 | 누가 | 폴더 | 브랜치 | 포트 |
|---|---|---|---|---|
| 메인서버 | 재권님이 보신다 | `C:\work\KJCStudio` | `main` | 8765 |
| 중간서버 | `홈페이지_정리` | `C:\work\kjc-staging` | `staging` | 8767 |
| 세션 | `주식페이지_개발` | `C:\work\kjc-stock` | `w-stock` | 8766 |
| 세션 | `주식페이지_개발1` | `C:\work\kjc-daily` | `w-daily` | 8768 |
| 세션 | `주식페이지_개발2` | `C:\work\kjc-home` | `w-home` | 8769 |
| 세션 | `주식페이지_개발3` | `C:\work\kjc-dev3` | `w-dev3` | 8770 |

**세션 서버 셋은 `--slow` 로 뜬다.** 확인용이라 KIS 를 느리게 부른다.
`startup.bat` 이 재부팅 때 다섯을 다 띄운다.

**세션이 없는 폴더는 뒤처진 채 멈춘다.** 받아갈 쪽이 없어서다 —
2026-09-21 에 `w-daily` · `w-home` 이 27건 뒤처져 있었고, 그날 그 자리에
세션 둘이 들어왔다.

**폴더 이름은 자리 이름이지 몫이 아니다.** `kjc-daily` 에 앉았다고 데일리분석만
맡는 것이 아니다. **네 번째(`kjc-dev3`)는 처음부터 세션 이름을 땄다** —
앞의 셋이 옛 구상의 이름이라 헷갈린 일이 있어서다 (2026-09-21). **맡는 곳은 「세션 역할 분담」 표에 적힌 것**이고 셋 다
`holdings/` 다. 폴더·브랜치·포트는 **「어느 자리에서 일하는가」** 를 가를 뿐이고,
자리가 나뉘어야 같은 파일을 동시에 고칠 수 있다. 그것이 이 구조의 전부다.

**2026-09-21 에 실제로 물어왔다.** 처음 구상에서 세션 이름을 `주식_데일리` ·
`주식_홈` 으로 잡고 폴더를 그에 맞춰 만들었는데, 재권님은 `주식페이지_개발1` ·
`2` 로 만드셨다. **폴더 이름만 옛 구상에 남았다.** 이름을 바꾸지 않는 것은
경로가 `startup.bat` · 워크트리 · 문서 여러 곳에 걸려 있어서다 —
**대신 이름이 몫을 뜻하지 않는다고 적어 둔다.**

**같은 레인에 여럿이면 공용 파일만 미리 정한다.** 화면별로 나누면 거의 안
부딪히는데(`index` · `stock` · `daily`·`news`), **여럿이 함께 쓰는 파일**
(`holdings/css/frame.css` · `holdings/js/components/*`)은 다르다.
2026-09-21 에 `주식페이지_개발` 이 종목 화면 때문에 `.kh-p3` · `.kh-dt` 를
`frame.css` 로 옮겼는데, **네 화면이 함께 읽는 파일**이라 셋 중 누가 만지면
다른 화면이 같이 움직인다. **누가 만질지는 `작업우선순위` 를 거쳐 재권님이 정하신다.**

**저장소 이력(`.git`)은 한 벌뿐이다.** `git worktree` 라 작업 파일만 따로 갖고
이력은 공유한다. 세션 하나 늘리는 값은 **6.3MB 쯤**이고 그중 대부분이
`market.db` 다 (2026-09-18 실측).

**로컬 서버는 독립된 창으로 띄운다.** 세션에 묶어 띄우면 그 세션이 쉴 때 같이 죽는다
(2026-09-18 에 두 번 그랬다). `startup.bat` 이 8765 를 띄우는 방식과 같다.

    start "제목" /min cmd /c "cd /d <폴더>\holdings && python server/kis_proxy.py --port <포트>"

### 검수에서 걸린 것은 보드에 쌓는다 (2026-09-18 지시)

중간서버 검수에서 어긋난 것은 **재권님께 가지 않고 보드에 쌓인다.**
`작업우선순위` 가 그것을 정리해 올리고, 재권님이 급한 순서를 정해 세션에 배분하신다.

    각 세션 ──▶ 중간서버 검수 ──┬── 통과 ──▶ 메인서버 ──▶ 재권님 확인
                                │
                                └── 어긋남 ──▶ **검수 보드**
                                                   │
                                        작업우선순위가 정리 ──▶ 재권님이 배분

**프로젝트 보드의 틀을 보고 만든다.** 칸반이 원래 「열림 → 진행 → 닫힘」 을
다루는 판이라 구조를 다시 짤 이유가 없다. 다만 **1606줄을 베끼는 일은 아니다** —
`ai-work` 을 같은 방식으로 만들었을 때 **이름이 같은 함수는 34개 중 8개**였다
(2026-09-18 실측). 나머지는 도메인이 달라 새로 썼다. 검수 보드도 담는 것이
다르므로(무엇이 어긋났나 · 어느 커밋 · 누가 고쳤나) 마찬가지다.

**인라인은 따라가지 않는다.** `projects/index.html` 이 1606줄인 것은 CSS·JS 가
전부 그 안에 있기 때문이다. `ai-work` 은 파일 셋으로 갈랐고, 그래서 색 검사가
`ai-work/css/*.css` 를 그냥 잡는다.

#### API 가 먼저다 — 화면이 아니다

    /api/board                 404
    /api/board/doc/projects    404      2026-09-18 실측. 8765 에 없다

**로컬 서버에 그 API 가 아예 없다.** 그래서 프로젝트 보드도 AI 작업 보드도
**로컬에서는** 브라우저 하나에만 저장된다. 배포본에는 `projects/worker/board-api.js`
가 있어 서버에 저장되지만, **그쪽은 Access 뒤라 세션이 못 쓴다.**
「저장은 이 PC 안에만」 이므로 **배포본 워커는 목표가 아니다.**

**새로 설계하지 않는다 — `board-api.js` 가 돌아가는 본이다.** 응답 모양을
그대로 흉내면 된다 (2026-09-18 실측).

    GET  /api/board/health          살아 있나
    GET  /api/board/doc/<이름>      문서 읽기
    PUT  /api/board/doc/<이름>      문서 쓰기      이름은 [A-Za-z0-9_-]{1,40}

자세한 것은 `projects/worker/README.md` 에 있다.

**이 상태로 화면을 먼저 만들면 「됐다」 로 보이는데 아무도 못 읽는다.**
`작업우선순위` 는 저장소 파일만 읽고, 다른 세션도 다른 PC 도 그 데이터를 못 본다.
**검수 보드의 목적이 통째로 사라진다.**

    순서   API 를 붙이고 되는 것을 확인한다 ──▶ 그 다음 화면

화면은 그 뒤면 오래 걸리지 않는다.

**항목을 지우지 않는다. 「닫힘」 으로 옮긴다 (2026-09-18 지시).**
지우면 **무엇이 자주 나는지 볼 수 없어 개선이 안 된다.** 누가 · 언제 ·
어떻게 고쳤는지 · 어느 커밋인지를 항목에 남긴다.

되풀이가 룰을 만든다. 2026-09-18 하루만 봐도 그랬다 —
「세면 나오는 값」 세 번 · 「목록이 갈림」 두 번 · 「값 굴리기」 네 번이
각각 룰 · 복제 제거 · 훅(`tools/hook-edit-churn.py`)이 됐다.
**몇 번 났는지 알았기 때문에 고칠 수 있었다.**

**재권님이 찾으신 건수가 줄어야 중간서버가 일하고 있는 것이다.**
이 보드가 그것을 재는 자리이기도 하다.

#### 저장은 이 PC 안에만 둔다 (2026-09-18 지시)

**git 에 올리지 않는다.** 나중에 맥미니에 서버를 세우면 **백업서버**를 만들어
거기서 관리한다. 그때까지는 이 PC 안이다.

그래서 설계에서 두 가지가 중요해진다.

| 무엇 | 왜 |
|---|---|
| **한 파일에 모은다** | 맥미니로 옮길 때 그 파일만 가져가면 된다 |
| **경로를 한 곳에서 정한다** | 서버가 생기면 그 한 곳만 고친다 |
| **저장할 때마다 직전 것을 남긴다** | git 이 없으니 **되살릴 방법이 그것뿐이다** |

마지막이 특히 그렇다. git 을 안 쓰면 실수로 지운 것을 되돌릴 수 없다.
`debugging/history/` 가 같은 방식으로 회차를 쌓고 있다 (161개, gitignore).

**이것은 화면 몫이 아니라 API 몫이다.** 브라우저는 파일을 못 쓴다.
`debugging/history/` 를 쌓는 것도 `check.py` 쪽이다. **PUT 을 받을 때
직전 것을 옮기면 된다.** 화면 쪽에 적어두면 아무도 못 한다.

**API 모양을 프로젝트 보드와 같게 둔다.** 맥미니 서버가 나중에 같은 모양으로
응답하면 **화면은 한 줄도 안 고치고** 주소만 바꾸면 된다. 경로도 `/api/board/doc/<이름>`
을 그대로 쓴다 — 모양이 같아야 아래 공용 모듈에 분기가 안 생긴다.

**저장 계층은 보드 셋이 함께 쓴다.** 프로젝트 · AI 작업 · 검수 보드로 **세 번째**라,
「같은 종류의 복제가 하나 더 나오면 합치거나 만든다」 에 걸린다.

    한 곳에 모을 것   persistLocal · loadFromServer · syncToServer · serverAlive · 경로

**합치는 것은 화면이 아니라 이 층 하나다.** 맥미니로 옮길 때 **그 한 파일만**
고치면 셋이 함께 따라온다. 지금 구조로는 세 군데를 고쳐야 한다.

#### 검수는 둘이 본다 — 감사를 따로 둔다 (2026-09-18 지시)

**`홈페이지_정리` 가 합치고 검수하고, `작업우선순위` 가 그것을 같이 본다.**

    홈페이지_정리   머지 · 검수 · 화면 확인 · 검사 실행
    작업우선순위    **그 검수를 검수한다** — 빠뜨린 것이 없나

**왜 둘인가** — 검수가 한 곳뿐이면 **그 자리가 놓치면 그대로 나간다.**
중간서버는 재권님 앞의 마지막 관문이라 특히 그렇다.

2026-09-18 하루만 봐도 **놓친 것을 찾아낸 쪽은 늘 다른 세션이었다.**

    세는 명령이 자기 문서를 세고 있던 것       홈페이지_정리가 찾음
    설계에서 API 를 화면보다 뒤에 둔 것         스케줄_툴개발이 찾음
    훅이 `daily-chart.js` 를 놓치던 것          주식페이지_개발이 찾음

**감사는 읽기만 한다.** `작업우선순위` 는 저장소 파일을 고치지 않는다.
어긋난 것을 찾으면 `홈페이지_정리` 에 알리고, 그쪽이 고치거나 해당 세션에 돌려보낸다.

### 세우기 전에 풀 것 — 홈페이지_정리와 검토 (2026-09-18)

**순서가 있다. 앞의 것을 안 풀고 뒤로 가면 구조가 안 돈다.**

| | 무엇 | 왜 먼저인가 |
|---|---|---|
| **1** | **5분봉 미리 받기를 갱신 시점에만** | 이것이 KIS 예산의 3분의 2를 쓴다. 안 고치면 **서버를 셋 이상 못 띄운다** |
| **2** | **`startup.bat` 에 중간서버(8767)를 더한다** | 8767 이 안 뜨면 「화면을 본다」 를 못 해 **구조의 이유가 없어진다** |
| **3** | `market.db` 를 한 벌로 공유 | 1 이 끝나야 한다. 쓰기가 여럿이면 WAL 이어도 부딪힌다 |

**세션이 서버를 띄우면 죽는다 (2026-09-18 실측).** 세 가지로 해봤는데 전부
죽었다 — 세션에 묶기 · `Start-Process` · `start /min cmd /c`.
**셋의 공통점은 세션이 부모라는 것이다.** 8765 가 종일 사는 이유는
`startup.bat` 이 띄웠기 때문이다. **세션이 서버를 관리하려 하지 않는다.**

**`market.db` 는 지금 `journal_mode = delete` 다** (4.2MB, 2026-09-18 실측).
이 모드로 공유하면 **쓰는 동안 읽기가 막힌다.** `WAL` 로 바꾸면 여럿이 읽고
하나가 쓰는 것이 되는데, **쓰는 쪽이 여럿이면 WAL 이어도 부딪힌다.**
그래서 1 → 3 순서다.

**예산 설정과 실제 사용량은 다른 이야기다.** 1 이 끝나 실제 호출이 줄어도
`budgetPerSec` 설정은 그대로라, 네 서버가 동시에 몰리면 한도를 넘을 수 있다.
**둘 다 봐야 한다.**

### 중간서버를 실제로 돌릴 때 (2026-09-18 · 홈페이지_정리 판단)

- **합칠 순서는 먼저 올린 순이다.** 급한 순으로 하면 「누가 급한가」 를
  합치는 쪽이 판정하게 되는데, 그것은 `작업우선순위` 자리다
- **뒤처짐은 개수로 재지 않는다.** 스무 커밋 뒤여도 남의 파일만 바뀌었으면
  문제가 없다. **「올리기 직전 merge」 가 지켜지면 뒤처짐 자체는 문제가 아니다**
- **되돌려보낼 때 `staging` 을 되감지 않는다.** 되감으면 통과한 남의 작업까지
  사라진다. **그 커밋만 `revert`** 하면 그 세션이 고쳐서 다시 올릴 때 깨끗하게 얹힌다
- **화면 확인은 고친 파일이 닿는 곳만 본다.** 「전체를 본다」 와 「고친 것만 본다」
  사이의 답이고, **기계가 고를 수 있다** — 화면이 무엇을 `import` 하는지 따라가면
  「이 파일을 고치면 어느 화면이 바뀌나」 가 나온다

      chart.js 를 고쳤다   → index · stock
      frame.js 를 고쳤다   → 화면 전부
      daily.js 를 고쳤다   → daily 하나

  2026-09-18 에 이것 때문에 두 번 놓쳤다 — 같은 버그가 `daily.js` 와 `news.js`
  양쪽에 있었는데 한쪽만 고쳤고, `chart.js` 를 고치면서 첫 화면만 보고
  종목 화면은 안 봤다. **도구로 만들 값이 있다** (`tools/`, 개념정의).

### 되돌리는 길

    git worktree remove <폴더>        세션 폴더마다
    git branch -D staging work1 …

**폴더가 하나로 돌아간다.** 다만 옮겨간 세션은 **장바구니(인덱스)도 나뉘므로**
아래 「장바구니도 하나다」 절이 그 세션에는 안 맞는다. **어느 세션이 옮겨갔는지
표에 적어 둔다.**

### 아직 안 정해진 것

- **임시서버 화면에 「정식 아님」 표시를 붙일지**
- 전환 시점. 전환하면 아래 「작업 폴더를 함께 쓴다」 절은 **낡는다** —
  장바구니 공유 · 남의 파일 건드리기 · 레인 섞인 커밋이 전부 그 절에서 나온 이야기다

## 메인 폴더는 셋이 함께 쓴다 (중요)

**`C:\work\KJCStudio` 한 폴더를 `개념정의` · `엔진_개발` · `작업우선순위` 가
공유한다.** 아래는 **그 폴더 안에서만** 참이다.

**세션 폴더를 가진 넷은 해당이 없다** — 각자 워크트리라 장바구니도 따로다.
2026-09-22 에 한 세션이 자기 폴더에서 두 번 커밋했고 **한 번도 안 섞였다.**
어느 폴더가 누구 것인지는 「세 자리로 나눈다」 의 표에 있다.

**2026-09-22 에 좁혔다 (지시).** 그 전에는 「한 폴더를 공유한다」 로 적혀 있었는데,
**전환이 일어났는데도 그대로였다.** 「세 자리로 나눈다」 안에 「전환하면 이 절은
낡는다」 고 적어 두고도 안 고쳤다.

**세션은 `staging` 에 직접 못 넣는다.** 커밋하고 알리는 데까지이고,
**`staging` 에 넣는 것은 창구가 `kjc-staging` 에서 머지한다.**

    git push . w-daily:staging   →  거부된다
                                    `kjc-staging` 이 그 브랜치를 체크아웃하고 있다

    (창구가) cd C:\work\kjc-staging && git merge w-daily

**이력에 남은 것도 그 모양뿐이다.** 2026-09-22 에 이것이 문서에 없어서
한 세션이 밀어 보고 거부당했다 — **적혀 있었으면 안 틀렸을 자리다.**

그래서 이렇게 된다.

- 내가 파일을 저장하면 **남의 `git status` 에 즉시 보인다.** 반대도 같다
- 내가 커밋하면 남에게도 **바로 반영된다.** `git pull` 로 주고받을 일이 없다
  (원격은 다른 PC·백업용이지, 세션끼리 주고받는 통로가 아니다)

### 절대 쓰지 않는 명령

| 명령 | 무슨 일이 생기나 |
|---|---|
| `git add -A` · `git add .` | 남이 편집 중인 미완성 파일이 **내 커밋에 딸려 들어간다** |
| `git checkout .` · `git restore .` | 남의 미커밋 작업을 **되돌려 없앤다** |
| `git reset --hard` | 위와 같다. **복구할 수 없다** |
| `git clean -fd` | 남이 아직 커밋 안 한 새 파일을 지운다 |

커밋할 때는 **경로를 반드시 지정한다.** `git commit -- <경로>` · `git add <경로>` 형태로 쓴다.

**이 표는 좁히지 않는다 (2026-09-22).** `deny` 가 **폴더와 무관하게** 막아서,
자기 폴더에 있어도 `git add -A` 는 막힌다. **이유는 달라졌지만**(남의 것이
섞여서가 아니라 습관으로) **금지는 그대로다.** 이유를 안 적으면
「폴더가 나뉘었으니 이제 써도 되나」 를 다시 묻게 된다.

**무엇이 막나 (2026-09-17)** — 위 명령들은 `.claude/settings.json` 의 `deny` 에 있다.
`deny` 는 `allow` 보다 먼저 평가되므로 **어디서 허용해도 막힌다.** 하위 명령에도 걸려서
`cd /tmp && git clean -fd` 도 멈춘다. 그 파일은 저장소에 담기므로 **두 PC 에 함께 적용된다.**

**다만 자물쇠가 아니라 걸림돌이다.** 공식 문서가 밝히듯 Bash 규칙은 프로그램에 대한
보안 경계가 아니다 — 이름을 바꿔 부르거나 형태를 바꾸면 빠져나간다.
흔히 쓰는 형태를 막아 **한 번 더 생각하게 하는 것**이 목적이다.

그 전에는 반대였다. `settings.local.json` 이 `Bash(git add:*)` 를 `allow` 에 두어
**`git add -A` 가 묻지도 않고 통과했다.** 문서로는 금지인데 설정으로는 무조건 허용이었다.

**배포하셨으면 세션에 말씀해 주십시오 (2026-09-17 지시).** 그 한마디를 들은 세션이
기록한다. 재권님이 명령을 외우실 일은 없다.

    git rev-parse @{u} > .claude/deployed

상태줄이 그 기록을 읽어 **워커가 그 뒤에 또 바뀌었는지**를 본다. 안 적으면
`배포 필요` 가 계속 떠 있는데, 실제로 안 한 것이니 맞다.
`.claude/deployed` 는 저장소에 담기지 않는다 — 배포는 PC 마다 따로 세는 것이 맞다.

**이 표시는 푸시 여부와 무관하다.** 처음에는 「푸시 대기」 안에 넣었다가 뺐다.
순서가 커밋 → 푸시 → **배포**라서, 푸시하는 순간 초록 「푸시 끝」 으로 바뀌며
안내가 사라졌다. **정작 배포할 때가 그때였다.** 2026-09-17 아침 배포본 `500` 이
바로 그 상태였다 — 푸시는 됐고 대시보드에만 안 올라가 있었다.

**커밋에 구역이 섞이면 멈춘다** — `.githooks/pre-commit` 이 본다. PC 마다 한 번 설치한다.

    git config core.hooksPath .githooks

**구역이 아니라 레인으로 센다.** 맡은 세션이 다른 레인이 한 커밋에 두 개 이상 담기면
멈추고 무엇이 걸렸는지 보여준다. 같은 레인의 여러 폴더를 함께 고치는 것은 정상이므로
지나간다(`tools/` 와 `.claude/` 는 둘 다 개념정의다). 루트 문서만 담긴 커밋도 지나간다.
레인 표에 없는 최상위 폴더는 `(미정)` 이라는 한 레인으로 묶는다.
일부러 섞어야 하면 `git commit --no-verify` 로 넘어간다.

**처음에는 구역 다섯 개만 셌고, 첫 실전에서 놓쳤다** — `temp/README.md` 가 남의
장바구니에서 딸려 들어왔는데 목록에 없는 폴더라 세지 않았다(`1a7c0df`).
그래서 레인 기준으로 바꿨다.

### 장바구니(인덱스)도 하나다 — 메인 폴더에서

`git add` 로 담아둔 것이 올라가는 자리를 인덱스라 한다. 폴더가 하나라 **이것도 공유된다.**

    주식페이지_개발이  git add holdings/...   → 장바구니에 10개
    스케줄_툴개발이    git add api-board/     → 3개 더
                       git commit             → 13개가 전부 커밋된다

**자기 경로만 담아도 남이 미리 담아둔 것이 함께 있다.** 2026-09-14 에 실제로 그럴 뻔했고,
커밋 직전에 확인해서 막았다.

- 커밋 직전에 `git diff --cached` 로 **무엇이 담겨 있는지 본다**
- 또는 `git commit -- <경로>` 를 쓴다. 이 형태는 **장바구니를 건너뛰고** 그 경로만 커밋한다
- 남의 것이 담겨 있으면 `git restore --staged <경로>` 로 내려놓는다.
  파일 내용은 그대로 남으므로 그 세션의 작업은 잃지 않는다

### 브랜치로는 못 나눈다 — 메인 폴더에서

폴더가 하나라 브랜치를 바꾸면 **모든 세션의 화면이 동시에 바뀐다.**
정말 나눠야 하면 `git worktree` 나 별도 클론을 써야 한다.
지금은 영역이 겹치지 않아 한 폴더로 간다 (2026-09-14 판단).

### 왜 적어두나

실제로 있었던 일 — `projects/` 가 다른 세션 커밋에 스테이징된 채 딸려 들어갈 뻔했다.
경로를 지정해 커밋해서 막았다. 되돌리기 명령은 그보다 나쁘다. 커밋 전이라
**되살릴 방법이 없다.**

## 작업 착수 규칙 (중요)
- **파일을 고치거나 만드는 일은 먼저 확인을 받는다.** 명령을 받아도 바로 코딩하지 않는다
  - 말할 것: 무엇을 고칠지 · 어떤 파일을 건드릴지 · 왜 그 방법인지 · 다른 방법이 있다면 무엇인지
  - 재권님이 "그렇게 해" 라고 하신 뒤에 코딩에 들어간다
- **조사 · 검색 · 화면 확인처럼 읽기만 하는 일은 바로 한다.** 확인받지 않아도 된다
  - 예: "API 가져올 수 있는 곳 알아봐", "호출 한도 확인해줘", "화면 어떻게 나오는지 봐줘"
  - 아무것도 바꾸지 않으므로 먼저 물으면 오히려 번거롭다
- 이유: 방향이 어긋난 채로 만들면 되돌리는 데 더 오래 걸린다 (2026-09-12 지시)

### 한 줄짜리는 바로 고친다 (2026-09-16 지시)

**위 「먼저 확인을 받는다」 의 예외다.** 일하다 찾은 것 중 **아래 셋을 모두
만족하면 지시 없이 고친다.**

    한 파일 안에서 끝난다
    되돌리기 쉽다
    다른 룰과 부딪히지 않는다

- **고쳤어도 `작업우선순위` 에 남긴다.** 「고쳤음」 으로 남긴다.
  남기는 것에는 예외가 없다 — 목록에서 빠지면 없는 일이 된다
- **커밋·푸시는 그대로 재권님 지시를 받는다.** 바뀐 것은 착수 여부 하나다
- 셋 중 하나라도 어긋나면 **목록에 올리고 기다린다**

**예외가 되는 이유** — 위 룰은 *방향이 어긋난 채로 만들면 되돌리는 데 더 걸린다*
는 것인데, **한 글자 고치는 것은 어긋나도 되돌리는 비용이 거의 없다.**

**왜 필요한가** — `holdings/status.html` 의 `/analysis/` → `/holdings/analysis/`
같은 것까지 재권님이 고르셔야 하면 **목록이 길어져 정작 큰 일이 묻힌다.**

### 지시가 룰과 어긋나거나, 룰이 없으면 — 먼저 알린다 (2026-09-15 지시)

재권님이 시키셨더라도 바로 손대지 않는다. 손대기 전에 두 가지를 본다.

1. **걸리는 룰이 있나** → 있으면 **어떤 룰인지 먼저 말한다.**
   그 위에서 어떻게 할지 안을 내고, 답을 듣고 들어간다
2. **룰이 아직 없나** → **룰부터 잡는다.** 정하고 나서 작업한다

시키신 대로 했다는 것이 면책이 되지 않는다. 룰을 아는 쪽은 이쪽이고,
재권님은 그때그때 모든 룰을 기억하고 말씀하시는 것이 아니다.
말없이 룰을 깨면 나중에 두 배로 고쳐야 한다.

**실제로 있었던 일** — 2026-09-15 에 "뉴스 카테고리마다 바탕색·글자색을
지정해 달라" 는 지시를 받았다. 그대로 CSS 에 색을 적었으면 룰 두 개를
깼을 것이다.

    색상 하드코딩 금지          색은 theme.css 변수로만 쓴다
    공유 자원은 바꾸기 전에 알린다  theme.css 는 네 구역이 함께 쓴다

그래서 코딩 전에 두 룰을 말씀드리고, 변수로 넣는 안을 만들어 승인받고,
`홈페이지_정리` 에 알린 뒤 진행했다. 재권님이 이 방식을 룰로 정하셨다.

**룰이 없어서 새로 잡은 것도 문서에 남긴다.** 말로만 정하면 다음에 또 묻는다.

### 시킨 방식이 안 되면 바꾸지 말고 말한다 (2026-09-17 지시)

**막히면 그 자리에서 말한다.** 다른 방법으로 만들어 놓고 나중에 밝히지 않는다.

    하지 않는다   PNG 가 안 되네 → 웹페이지로 만들어 드림 → 물으시니 그때 밝힘
    이렇게        PNG 가 두 번 다 멈춥니다. 웹페이지로 할까요?

- **두 번 해서 안 되면 세 번째 대신 말한다.** 세 번째도 안 될 확률이 높다
- **「더 나은 방법이 있다」 도 같다.** 나은지 아닌지는 재권님이 정하신다
- **어쩔 수 없이 바꿔서 냈으면 결과물 첫 줄에 적는다.**
  「이미지로 요청하셨는데 웹페이지로 만들었습니다 — 이유는 △△」
  **물으시기 전에** 적는다

**왜** — 바꾸는 쪽에서는 「막혔다」 가 아니라 「해결했다」 로 느껴진다.
안 된다고 말하는 것은 실패 보고 같고, 다른 방법으로 내놓는 것은 일한 것 같다.
그래서 악의가 아니라 그 반대다 — 어떻게든 결과를 드리려는 것이다.
**그런데 그 결과물은 시키신 것이 아니다.**

**말하는 데 3초, 다른 것을 만드는 데 몇 분이 든다.** 묻는 쪽이 늘 빠르다.

바로 위 「지시가 룰과 어긋나거나」 와 짝이다. 그것은 **룰**과 부딪힐 때이고,
이것은 **방법**이 막힐 때다.

**전부는 기계가 못 잡는다.** 결과물만 보면 무언가 나와 있어서 검사기는 통과시킨다.

**다만 한 가지 형태는 잡는다 (2026-09-17).** 같은 줄을 값만 바꿔가며 되풀이해
고치는 것 — `tools/hook-edit-churn.py` 가 `Edit` 마다 돌며 **10분 안에 같은 모양이
세 번**이면 알린다. 막지는 않는다.

    height: 1000px → 500px → 700px      ← 숫자를 지우면 같은 모양이다

2026-09-17 에 한 줄이 네 번 바뀐 일에서 나왔다. 원인은 그 줄이 아니라 재권님이
다른 파일을 보고 계셨던 것이었다. **첫 번째에 안 됐을 때 말했으면 네 번 굴리지 않았다.**

**2026-09-17 에 세 세션이 다 걸렸다.** 한 세션의 문제가 아니다.

### 내 레인 밖의 일을 시키시면 — 하지 않고 되묻는다 (2026-09-16 지시)

재권님이 시키셨더라도 **그 자리가 내 레인이 아니면 손대지 않는다.**
바로 하지 말고 **어느 세션 담당인지 알려드린 뒤 답을 듣는다.**

    "그 파일은 `주식페이지_개발` 레인입니다. 넘길까요, 아니면 제가 할까요?"

**왜** — 재권님은 그때그때 레인 표를 기억하고 말씀하시는 것이 아니다.
표를 아는 쪽은 이쪽이다. 시키신 대로 했다는 것이 면책이 되지 않는 것은
위 「지시가 룰과 어긋나거나」 와 같다.

- **레인 표에 없는 자리도 마찬가지다.** 「아마 내 것이겠지」 로 넘어가지 않는다.
  정해지지 않았으면 정하고 나서 손댄다
- 넘기기로 하면 **받는 세션에 무엇을 왜 해야 하는지까지 적어 보낸다.**
  "이거 해달라" 한 줄만 던지지 않는다
- 급히 깨져 있는 것은 예외다. 배포본이 안 뜨거나 값이 안 나오는 것처럼
  재권님이 보시는 화면이 망가진 상태면 레인을 따지기 전에 먼저 알린다

## 커밋 · 푸시 · 배포 규칙 (중요)
- **커밋과 푸시는 재권님이 하라고 하실 때만 한다.** 알아서 올리지 말 것

### 세 단계는 순서대로 간다 (2026-09-16 지시)

    커밋  →  푸시  →  배포

**각각 별개의 일이고, 앞 단계가 뒷 단계를 자동으로 시키지 않는다.**

| 단계 | 무슨 일인가 | 누가 볼 수 있나 |
|---|---|---|
| **커밋** | 이 PC 의 저장소에 기록을 남긴다 | 같은 폴더를 쓰는 세션들만 |
| **푸시** | GitHub 에 올린다 | 다른 PC · 백업 |
| **배포** | **실제 사이트에 반영한다** | 재권님과 접속 허용된 사람 |

### 푸시가 배포를 대신하는 것과 아닌 것

**여기가 갈린다.** 무엇을 고쳤느냐에 따라 다르다.

| 무엇을 고쳤나 | 배포 방법 | 푸시하면 |
|---|---|---|
| 화면 — HTML · CSS · JS · `data/*.json` | GitHub Pages 자동 | **반영된다** |
| `holdings/worker/kis-worker.js` | Cloudflare 대시보드에 붙여넣기 | **아무 일도 안 일어난다** |
| `projects/worker/board-api.js` | Cloudflare 대시보드에 붙여넣기 | **아무 일도 안 일어난다** |

**워커는 저장소와 연결되어 있지 않다.** `wrangler` 설정이 저장소 어디에도 없다
(2026-09-16 확인). 대시보드에서 손으로 붙여넣는 방식이다.

### 워커를 고쳤으면 배포까지가 한 덩어리다

푸시만 하고 끝내면 **로컬과 배포본의 동작이 갈린다.** 화면은 멀쩡해 보이는데
값만 다르다. 눈으로는 못 찾는다. 2026-09-15~16 에 이것으로 여러 번 깨졌다.

    1. Cloudflare 대시보드 → Workers & Pages → 해당 Worker
    2. Edit code
    3. 편집기 내용을 전부 지우고 파일 전체를 붙여넣기
    4. Deploy

| 파일 | 올라가는 Worker |
|---|---|
| `holdings/worker/kis-worker.js` | `kjc-kis-proxy` |
| `projects/worker/board-api.js` | `kjc-board-api` |

붙여넣은 뒤 **맨 아래로 내려 끝이 `};` 인지 본다.** 중간에 잘리면 워커가
통째로 죽는다. 절차 전체는 `holdings/worker/README.md` ·
`projects/worker/README.md` 에 있다.

### 배포는 재권님이 한다

Cloudflare 대시보드는 세션이 들어갈 수 없다. **그래서 워커를 고친 세션은
「무엇을 어느 워커에 올려야 하는지」를 알려야 한다.** 푸시했다고 끝난 것이 아니다.

### 배포 확인 — `302` 는 실패가 아니다

- 사이트 전체가 **Cloudflare Access 뒤에 있다.** 로그인하지 않은 요청은
  `/api/*` 까지 포함해 전부 302(로그인 화면으로 보냄)가 된다
- **그래서 `curl` 이나 스크립트로는 확인이 안 된다.** 302 가 나왔다고 배포가
  실패한 것이 아니다. 확인은 **브라우저에서 로그인한 상태로** 한다

2026-09-16 실측이다.

    wornjs2003.github.io/kjcstudio/   301 → thekjcstudio.com
    thekjcstudio.com/                 302 → Access 로그인
    thekjcstudio.com/api/kis/health   302 → Access 로그인

### 커밋 이야기는 **화면 확인 다음**에 꺼낸다 (2026-09-14 지시)

작업이 끝나면 **먼저 띄워서 보여드린다.** 커밋 이야기는 재권님이 눈으로 확인하신 뒤다.

    1. 작업 끝남
    2. 서버 띄우고 브라우저 열기 (이미지·문서는 뷰어로)
    3. "이러이러하게 바뀌었습니다. 확인해 주세요"
    4. 재권님 확인
    5. 그다음에야 "커밋할까요?"

**검증을 통과했다는 것과 재권님이 원하신 대로 나왔다는 것은 다르다.** 문법이 맞고 변수가
빠지지 않았어도, 화면이 생각과 다르면 고쳐야 한다. 그 상태로 커밋하면 되돌리는 커밋이
하나 더 붙는다.

2026-09-14 에 여러 세션이 작업 직후 바로 "커밋할까요" 를 물어서 이 규칙이 생겼다.
순서가 뒤집혀 있었다.

- 답을 듣기 전에는 올리지 않는다
- 아래 "작업 환경" 의 `git push` 는 PC 를 옮길 때 동기화하라는 뜻이지,
  매번 알아서 올리라는 뜻이 아니다 (2026-09-12 지시)
- 이유: 원격을 다른 세션·다른 PC 와 함께 쓰기 때문에, 올리는 시점은 재권님이 정한다

## 작업 환경 (2대 병행)
- Mac 과 Windows PC(`C:\work\KJCStudio`) 양쪽에서 작업. 동기화는 GitHub 저장소 기준
- 다른 PC 로 옮겨 작업 시작 전 `git pull`, 끝나면 `git push` 로 맞출 것
- 줄바꿈은 `.gitattributes` 로 LF 고정 (`.bat` 만 CRLF). OS 바꿔도 전체 파일이 변경된 것처럼 보이지 않음
- `tools/refresh-works.py` 는 `newline="\n"` 으로 저장하므로 Windows 에서 돌려도 diff 가 깨지지 않음

## 이미지 최적화 권장
- 포맷: JPG (사진/렌더) · PNG (투명도 필요 시) · WebP (최고 압축률)
- 가로 크기: 1200~1800px (매저너리 그리드 2~3열 기준 충분)
- 파일 크기: 장당 500KB 이하 권장 (GitHub Pages 로딩 속도)
- 색공간: sRGB

## 응답 규칙
- 사용자 호칭은 항상 "재권님"으로 부를 것
- 항상 존댓말로 답변할 것
- 영어 혼용 금지, 한국어로만 답변할 것
- 생각중·작업중·수정중 등 모든 진행 상태 표시 메시지는 영어 금지, 반드시 한국어로 표시할 것
- 파일/코드 편집, 검색, 도구 호출 등 내부 작업 라벨도 모두 한국어로 표시할 것
- **숫자에는 뜻을 붙인다.** 포트·상태코드·한도 같은 숫자는 처음 나올 때 괄호로 뜻을 적는다.
  숫자만 던지면 재권님이 되물어야 한다 (2026-09-15 지시)
  - `8765(holdings 미리보기)` · `404(그런 주소 없음)` · `500(서버가 터짐)` ·
    `020(OpenDART 하루 한도 초과)` · `1101(워커가 예외로 죽음)`
  - 답변 · 세션 간 메시지 · 화면 문구 · 오류 메시지에 적용한다.
    특히 화면 문구다 — 숫자만 보이면 정상인지 아닌지 판단할 수 없다
  - **커밋 해시도 마찬가지다 (2026-09-18 지시).** `74b6a1c` 만 적으면 읽을 수 없다.
    **무엇인지 함께 적는다**

        하지 않는다   `74b6a1c` 를 푸시할까요?
        이렇게        `74b6a1c`(차트 세로 룰) 를 푸시할까요?

    표에서도 같다 — 「하는 일」 칸에 해시만 있으면 무슨 일이었는지 모른다.
    **해시는 어느 것인지 가리키는 이름표지 내용이 아니다**
  - 같은 글 안에서 반복되면 처음 한 번만 붙인다
  - 코드 자체(`if (res.status === 404)`)에까지 매번 주석을 달 필요는 없다
  - **값 자체는 여기 적지 않는다.** 포트는 「실행 커맨드」, API 한도는 `/api-board/`,
    공시 한도는 `holdings/CLAUDE.md` 에 있다. 여기 또 적으면 바뀔 때 세 곳을 고쳐야 한다
    (「같은 값은 한 곳에만 둔다」)

## 추측 금지 규칙 (중요)
- **추측으로 안내하지 말 것.** 특히 외부 서비스(Cloudflare, GitHub, KIS 등)의 화면 경로·메뉴 이름·설정 위치
- 안내 전에 반드시 공식 문서 등 최신 출처를 확인하고, **확인한 내용만** 제공할 것
- 확인하지 못한 항목은 "확인하지 못했다"고 명시할 것. 그럴듯하게 지어내지 말 것
- 확인한 것과 확인하지 못한 것을 답변에서 구분해서 표시할 것
- 화면을 직접 볼 수 없는 작업은 재권님께 보이는 항목을 물어보고 그 기준으로 안내할 것
- 이유: 틀린 경로를 안내하면 재권님이 헤매게 되고 시간이 낭비된다 (2026-09-10 지시)

## 작업 확인 규칙 (중요)
- 작업을 시키면 확인을 꼭 한다.
- 작업의 완성을 재권님이 확인할 수 있도록 한다.
- 작업의 완성을 확인한다는 것은 "실행을 해서 재권님이 눈으로 볼 수 있어야 한다"는 뜻이다.
- 코드만 작성하고 "되어 있을 것입니다"로 끝내지 말 것.
- 코드 수정 후에는 반드시 재권님이 화면으로 결과를 확인할 수 있는 상태(실행 중인 페이지, 열려 있는 브라우저 등)까지 만들어서 넘길 것.
- 실행 환경이 막히면 대안을 끝까지 찾아서 재권님이 결과를 볼 수 있게 할 것 (로컬 서버, 단일 HTML, 스크린샷, 직접 열어주기 등).
- **이미지·시안·문서를 만들면 경로만 알리지 말고 항상 화면에 띄워서 보여줄 것** (2026-09-14 지시)
  - 터미널에 그린 것은 재권님 화면에 안 보일 수 있고, 경로만 드리면 직접 찾아 여셔야 한다
  - 만드는 것과 띄우는 것까지가 한 덩어리다. 이미지는 브라우저나 기본 뷰어로, 문서는 메모장 등으로 연다
  - 저장소에 남길 것이 아니면 세션 임시 폴더(scratchpad)에 만들고 거기서 띄운다

### 재권님이 확인하실 것은 **맨 아래**에 둔다 (2026-09-17 지시)

「푸시할까요」 · 「이렇게 보이는 게 맞습니까」 처럼 **재권님이 답하셔야 하는 것**은
답변의 **맨 마지막**에 두고, 위아래를 줄로 감싼다.

    ────────────────────────────
    ▶ 푸시 지시를 주시면 올리겠습니다 (커밋 2건)
    ────────────────────────────

**왜 맨 아래인가** — 터미널은 **끝부분이 화면에 남는다.** 위에 두면 긴 답변에서는
스크롤을 올려야 보인다. 줄로 감싸는 것은 글이 길 때 눈에 걸리게 하기 위해서다.

- **하나만 둔다.** 여럿이면 무엇부터 답할지 재권님이 고르셔야 한다
- 설명은 그 위에 쓴다. 확인할 것 안에 설명을 섞지 않는다

**본문에서 묻지 않는다 (2026-09-22 지시).** 「맨 아래에 둔다」 만 적혀 있어서
**본문에서 묻고 아래에 안 두면 어긴 것이 아닌 것처럼** 읽혔다.

    하지 않는다   … 그 세션에 알릴까요, 아니면 먼저 확인하시겠습니까 …
    이렇게        본문에는 안 묻는다. **맨 아래 한 자리에만** 둔다

**2026-09-22 에 실제로 그랬다.** 본문 한복판에서 **둘을 물었고**, 재권님이
답을 안 주신 채 같은 것을 한 번 더 말씀하셨다. **묻는 자리를 틀리게 잡아
한 번 더 말씀하시게 했다.**

**물을 것이 여럿이면 하나만 묻고 나머지는 미룬다.** 「하나만 둔다」 는
**고르시게 하지 않는다**는 뜻이다. 둘 다 급하면 **더 급한 것을 묻고, 나머지는
「그 다음에 여쭙겠습니다」 로 본문에 적는다** — 그것은 묻는 것이 아니라 알리는 것이다.

**가르는 기준 한 줄 — 재권님이 답하셔야 하면 묻는 것이다 (2026-09-22).**

| | 예 | 어디에 |
|---|---|---|
| **묻는 것** | 「A 로 할까요」 · **「A 가 맞다고 봅니다. 다르게 보시면 말씀해 주십시오」** | **맨 아래 확인란** |
| 알리는 것 | 「그 다음에 여쭙겠습니다」 · 「창구에 올렸습니다」 · 「띄워 두었습니다」 | 본문 |

**낱말로는 안 갈린다.** 「할까요」 가 없어도 **답을 기다리고 있으면 묻는 것**이다.
「다르게 보시면 말씀해 주십시오」 는 물음표가 없지만 **재권님이 보고 판단하셔야** 한다.

**답할 것이 없으면 확인란을 두지 않는다.** 그 자리는 「답하셔야 하는 것」 자리인데
안내를 넣으면 **「무엇을 답하라는 거지」 가 된다.** 그날 한 세션이
「…을 열어 보시면 됩니다」 를 확인란에 넣었다가 스스로 찾았다.

**세기 전에 그 검사가 도는지 먼저 본다.** 「할까요」 로 세어 `0` 이 나와도
**「안 물었다」 인지 「그 낱말로만 안 물었다」 인지 갈리지 않는다.**
**확인란이 몇 개인지 먼저 세어 `0` 이 아닌 것을 확인한 뒤** 본문 질문을 센다 —
그것이 `0` 이면 **가르는 기준 자체가 안 먹는 것**이다.

그날 세 세션이 세는 방법에서 **각각 다르게 틀렸는데**, 이 방법만 안 틀렸다.

    한 세션   `---` 만 보고 박스 문자를 못 봐 **199** (실제 3, 전부 오탐)
    나         마흔 개만 좁게 세어 **1** (전체를 넓게 세니 29)
    한 세션   「여쭙」 을 넣어 **2** (둘 다 남이 묻는 것을 적은 서술)

**주소는 백틱으로 감싼다 (2026-09-22 지시).**

    이렇게        ▶ `http://localhost:8766/holdings/` 을 열어 확인해 주십시오
    하지 않는다   ▶ http://localhost:8766/holdings/ 을 열어…
    하지 않는다   ▶ [주식 첫 화면](http://localhost:8766/holdings/) 을 열어…

**재권님 화면에서 백틱으로 감싼 것만 파랗게 눌린다.** 그날 한 세션이 네 형태를
나란히 놓고 여쭈어 갈렸다 — 맨 주소 · 마크다운 링크 · **백틱** · 꺾쇠 중 백틱이었다.

**마크다운 링크를 쓰지 않는다.** 웹에서는 그것이 표준이라 손이 그쪽으로 가는데
여기서는 안 파랗다. 맨 주소도 안 된다 — 재권님이 **직접 복사해 주소창에 넣으셔야
한다.**

**짐작으로 두 번 고치다 세 번째에 여쭈어 한 번에 갈렸다.** 「시킨 방식이 안 되면
바꾸지 말고 말한다」 가 여기서 들었다 — **형태 넷을 나란히 놓고 물은 것**이
짐작 세 번보다 빨랐다.

**줄을 코드블록으로 만들지 않는다 (2026-09-22 확인).**

    이렇게        `────` 를 **글자로 직접** 적는다
    하지 않는다   세 겹 백틱으로 감싼다 — **그 안은 주소를 백틱으로 써도 안 눌린다**

그날 한 세션이 「줄로 감싼다」 를 **코드블록으로 구현했다.** 백틱 룰을 지켰는데도
재권님이 **「도대체 왜 안하지」** 하셨다. **두 룰이 부딪힌 자리다** — 「줄로
감싼다」 가 「백틱으로 감싼다」 를 무력화했다.

**네 형태를 나란히 놓고 여쭈어 갈렸다.**

    ① 글자 줄(`────`) 사이     **눌린다**
    ② 코드블록 안              안 눌린다
    ③ 줄 없이 그냥             안 눌린다
    ④ 인용(`>`) 안             안 눌린다

**또 형태를 나란히 놓는 것이 빨랐다.** 앞서 백틱을 가를 때도 그랬다 —
짐작으로 고치면 「지켰는데 왜 안 되지」 가 반복된다.

**왜 그런지는 모른다.** 화면을 우리가 못 보므로 「그렇게 보인다」 까지가
확인한 전부다.

**무엇이 검사하나** — 아직 없다. **답변 기록에서 셀 수 있다.**

    「할까요 · 하시겠」 가 **본문에 있는데** 맨 아래 확인란이 없는 것
    `▶` 가 있는 줄에서 **백틱 없는 `http`**

2026-09-22 에 앞엣것으로 세어 최근 마흔 개 중 하나를 찾았다.
**다만 기록이 세션마다 따로라 자기 것만 셀 수 있다** — 각 세션이 스스로 센다.

### 고친 그 화면을 띄운다 (2026-09-18 지시)

**보기 좋은 화면을 고르지 않는다.** 고친 것을 보여드릴 때는 **고친 그 자리**를 띄운다.

**실제로 있었던 일** — 첫 화면의 배치를 바꿔 놓고 **종목 화면**을 띄웠다.
「차트가 커서 범례가 잘 보인다」 는 이유였는데, 종목 화면은 **안 고친 쪽**이라
재권님께는 **아무것도 안 바뀐 것으로 보였다.**

- 고친 파일과 띄우는 화면이 **다르면 띄우기 전에 말한다** —
  「고친 것은 A 인데 B 를 띄웁니다. 이유는 △△」
- **한 화면만 고쳤으면 그 화면만 띄운다.** 다른 화면이 옛 모습인 것은 정상이고,
  그 사실을 함께 말해야 재권님이 「덜 됐다」 로 읽지 않으신다
- 여러 화면에 걸쳐 고쳤으면 **어디가 바뀌었는지 먼저 말하고** 띄운다

**왜** — 띄우는 쪽은 「잘 보이는 것」 을 고르고, 보시는 쪽은 「고친 것」 을 보신다.
그 둘이 갈리면 **일한 것이 안 한 것처럼 보인다.**
「시킨 방식이 안 되면 바꾸지 말고 말한다」 와 같은 자리다 — 말없이 바꾸면
결과물이 시키신 것과 달라진다.

### 「됨」 이라고 보고하지 않는다 — 넣은 값을 적는다 (2026-09-18 지시)

지시 다섯을 「됨」 다섯으로 보고했는데 재권님이 화면을 보시고 **둘이 안 됐다**고
하셨다. 「띠가 그려졌는가」 는 봤고 **「지시하신 색인가」 는 안 봤다.**

    지금     | RSI 30~70 색 | 됨 |
    이렇게   | RSI 30~70 색 | 지시 **옅은 파랑** / 넣은 값 `#9b51e0` **보라** |
                                          └ 쓰는 순간 걸린다

- **값이 있는 지시(색 · 숫자 · 크기 · 방향)는 「됨」 대신 넣은 값을 적는다**
- **지시하신 낱말을 그대로 옆에 놓는다.** 「파랑」 과 `#9b51e0` 을 나란히 쓰면
  대조가 저절로 된다. 기억이 아니라 **쓰는 칸이 강제한다**

**값이 있는 지시는 먼저 변수를 찾는다.** 이번 건의 가장 싼 원인이 여기였다 —
재권님이 말씀하신 파랑이 **이미 `theme.css` 에 있었다.**

    --kh-rsi-band-rgb: 49 130 246     ← #3182f6. 지시하신 그 색
    시안                rgba(155,81,224,.09)   ← 보라를 직접 박았다
    시안이 그 변수를 쓴 횟수                    0

「색상 하드코딩 금지」 만 지켰어도 **지시한 색이 저절로 나왔다.**
`temp/` 는 색 검사 범위 밖이라 그 위반이 안 걸렸다.

**참고 이미지가 지시와 어긋나면 지시를 따르고, 어긋난다는 것을 먼저 말한다.**
시안 설명글이 스스로 「30~70 이 **옅은 보라**」 라고 적어 놓고 「됨」 으로
보고했다. 참고 이미지에 보라로 보였을 것이고, **이미지가 지시를 이겼다.**
「시킨 방식이 안 되면 바꾸지 말고 말한다」 와 같은 계열인데, 그것은 **방법이
막힐 때**고 이것은 **참고물이 지시와 다를 때**다.

**대조할 자리를 셋으로 두지 않는다.**

    재권님 지시문    "30~70 색 옅은 파랑으로"     ← 저장소에 없다
    시안 설명글      "30~70 이 옅은 보라"
    시안 코드        rgba(155,81,224,.09)

**뒤 둘은 서로 맞는데 둘 다 지시와 어긋났다.** 자기들끼리 일관되면 안에서
보기에 멀쩡하다. 그래서 **지시하신 낱말을 보고에 적어 세 번째 자리를 만든다.**

**아직 검사가 없다.** 지시가 대화에만 있어 기계가 볼 것이 없다. 다만 위
「변수를 먼저 찾는다」 는 색 검사가 잡을 수 있다 — `temp/` 를 범위에 넣으면 된다.

### 무엇을 띄우고 무엇을 안 띄우나 (2026-09-16 지시)

| 유형 | 방법 |
|---|---|
| **① 이미지** — 시안 · 스크린샷 | **띄운다** |
| **② 로컬** — `localhost:8765` 의 실제 화면 | **띄운다** (브라우저는 이미 열려 있다) |
| **③ 웹** — 배포본 `thekjcstudio.com` | **띄운다** |
| **④ 설명** — 내가 쓴 조사·분석·정리 | **대화에 쓴다.** 표가 크면 그때만 문서로 |

**④를 띄우지 않는 것이 핵심이다.** 위 「항상 띄워서 보여줄 것」 은 ①②③ 을 두고 한 말이다.
원래 취지가 **「코드만 짜놓고 되어 있을 것입니다로 끝내지 마라」** 이기 때문이다.
내가 쓴 설명은 작업물이 아니라 **작업에 대한 말**이고, 재권님은 어차피 대화를 보고 계신다.

**실제로 있었던 일** — 2026-09-16 에 조사 결과를 HTML 로 만들어 브라우저로 띄우기를
네 번 했다. 같은 날 `CLAUDE.md` 창이 16개까지 쌓여 재권님이 "이 창 누가 띄웠냐" 고
물으셨다. 창을 다루는 도구의 안전장치를 하루 종일 다듬었는데,
**부르는 횟수를 줄이는 쪽이 먼저였다.**

④ 를 문서로 만들어도 되는 경우는 이렇다.

- 표가 여러 겹이라 터미널에서 읽기 어려울 때
- 나중에 다시 꺼내 보셔야 할 때
- 그림·도표가 섞일 때

다른 기기에서 보셔야 하면 창을 띄우는 대신 **파일만 보내는 수단**을 쓴다.

### 창을 띄울 때 기존 창을 건드리지 않는다 (2026-09-16 지시)

**무엇을 무엇으로 여는지**가 먼저다.

| | |
|---|---|
| **① 띄우는 것** | 로컬 이미지 · 로컬 웹페이지 |
| **② 문자로 된 것** | **메모장으로 연다** |
| **③ 앞으로 꺼낼 때** | **그 프로그램의 창만 본다. 제목으로 짐작하지 않는다** |

**③ 이 따로 있는 이유** — 버그가 여는 자리가 아니라 **연 다음**에 있다.
이미지와 웹페이지는 새 창이 생겨서 「열기 전/후」 비교로 잡히는데,
**메모장은 탭으로 붙어서 새 창이 안 생긴다.** 그러면 제목으로 짐작하러 가고,
거기서 샌다. 2026-09-16 실측이다.

    'kis-worker.js' 로 찾음 → 1개 일치
    → kis-worker.js - KJCStudio - Visual Studio Code

**제목은 그 파일을 연 다른 프로그램도 갖고 있다.** 편집기에 열어 둔 파일을
띄우면 재권님이 쓰시던 편집기가 앞으로 튀어나오고, 1개라서 조건도 통과한다.
② 때문에 문자는 **항상** 이 길로 가므로 오히려 더 자주 난다.

같은 날 이 자리를 두 번 좁혔다 — **임의의 힌트 → 파일명 → 프로그램 + 파일명.**
앞의 둘은 **무엇으로 열었는지를 안 보고** 제목만 봤다는 점이 같았다.

프로세스 **이름**으로 거른다. `Popen` 이 준 PID 로는 안 된다 — 메모장이 탭
방식이라 그 프로세스는 기존 메모장에 넘기고 곧 끝나서, 쥐고 있던 PID 는
죽어 있고 그 PID 의 창도 없다.

새 창을 앞으로 꺼내는 것은 되지만, **재권님이 보고 계시던 창에는 손대지 않는다.**

- **기존 창을 최소화하지 않는다**
- **기존 창을 옮기거나 크기를 바꾸지 않는다**
- **제목으로 못 찾으면 아무 창도 건드리지 않는다**
- **기존 창을 닫지 않는다**
- **이미 띄운 것은 다시 열지 않는다.** 앞으로만 꺼내고 새로고침을 부탁드린다

세 번째가 핵심이다. 2026-09-16 에 제목 매칭이 빗나갔는데, 그때 「가장 최근에 뜬
Chrome」 을 대신 집어서 **재권님이 보고 계시던 Cloudflare 대시보드를 앞으로
꺼냈다.** 못 찾았으면 멈췄어야 했다.

**찾지 못하는 것은 괜찮고, 엉뚱한 창을 집는 것이 사고다.**

`SW_RESTORE` 는 **방금 내가 연 창에만** 쓴다. 그 창이 최소화된 채 열렸을 때
펴 주는 용도이고, 다른 창에는 쓰지 않는다.

#### 누가 띄운 창인지 제목에 남긴다 (2026-09-17 지시)

창을 여럿 띄워 두면 **어느 세션이 띄운 것인지 알 수 없다.** 2026-09-16 에
`CLAUDE.md` 창이 16개 쌓여 재권님이 "이 창 누가 띄웠냐" 고 물으셨다.

파일명 규칙(`temp/README.md`)만으로는 모자란다. **브라우저 탭에 뜨는 것은
파일명이 아니라 `<title>` 이다.**

`tools/show-window.py` 가 **알아서 붙인다.** 기억하지 않아도 된다.

    <title>지수 정리</title>   →   <title>개념정의 · 지수 정리</title>

- 세션 이름은 환경변수 `CLAUDE_CODE_SESSION_ID` 로 기록 파일을 찾아 읽는다.
  `tools/update-sessions.py` 와 같은 방식이다
- **저장소 파일은 고치지 않는다.** 화면에 나가는 제목이고 우리 것이 아니다.
  `temp/` 와 세션 임시 폴더만 손댄다. 그 밖이면 알리기만 한다
- 이미 붙어 있으면 그대로 둔다

**직접 열지도, P/Invoke 를 부르지도 않는다. 이것을 쓴다.**

    python tools/show-window.py <파일> [창제목의 일부]

위 「하지 않는 일」 이 이미 들어 있다. 제목이 **하나로 특정되지 않으면 아무 창도 건드리지 않는다** —
0개여도, 다섯 개가 걸려도 멈춘다. 직접 쓰면 그때마다 다시 지켜야 하고,
실제로 그러다 남의 창을 앞으로 꺼냈다.

**기억해야 지켜지는 룰은 지켜지지 않는다.** 그래서 도구로 옮겼다
(「같은 값은 한 곳에만 둔다」 의 대조 도구와 같은 생각이다).

#### 이미 띄운 것을 다시 보여드릴 때는 새로 열지 않는다 (2026-09-18 지시)

**다섯 번째가 오늘 생겼다.** 그 전에는 네 번째가 「닫지 않는다. **항상 새 창으로
연다**」 한 줄이었다. **둘은 다른 이야기인데 한 줄에 묶여 있었다** —
닫지 않는 것은 재권님 창을 지키려는 것이고, 매번 새로 여는 것은 그 결과가 아니다.

그 한 줄이 **창을 쌓으라고 시키고 있었다.** 그래서 「새 탭 그만 열겠습니다」 는
지킬 수 없는 약속이었다 — 지키면 룰을 어기는 상태였다.

    2026-09-16   CLAUDE.md 창 16개. 재권님이 "이 창 누가 띄웠냐" 고 물으셨다
    2026-09-18   md-diff 창 4개. 힌트 없이 못 찾아 다시 부른 것이 쌓였다

**이미 떠 있으면 `tools/show-window.py` 가 새로 열지 않는다.** 앞으로만 꺼내고
이렇게 알린다. 기억하지 않아도 된다.

    이미 떠 있어 앞으로만 꺼냈습니다: …
    **파일이 바뀌었으면 그 창에서 Ctrl+Shift+R 을 눌러 주십시오.**

**브라우저를 새로고침시킬 방법이 없다.** 주소를 여는 것밖에 못 해서, 이미 열린
탭을 갱신하려면 확장이나 디버깅 포트가 있어야 한다. 둘 다 안 열어 두었다.
**그래서 새로 여는 대신 「눌러 주십시오」 라고 말씀드리는 것이 맞다.**

같은 창이 **여럿** 떠 있으면 아무것도 열지 않고 몇 개인지 알린다. 이미 쌓여
있다는 뜻이라, 거기에 하나 더 얹지 않는다.

**여는 것도 도구로 한다.** `Start-Process <주소>` 나 `os.startfile` 을 직접
부르면 이 장치를 통째로 건너뛴다. 아래 「직접 P/Invoke 를 부르지 않는다」 는
**창을 조작하는 것**만 말하고 있었는데, **여는 것**이 빠져 있어서 그 길로 샜다.

**네 번째는 한 번 넣었다가 뺐다 (2026-09-16 지시).** 같은 파일을 두 번 띄우면
창이 쌓여서, 제목이 맞는 창을 닫고 새로 여는 방식을 넣었었다. **그 안전장치가 샜다.**

    힌트 '주식페이지_개발'   1개 일치 → 통과 → 재권님 세션 창
    힌트 'KJC'              3개 일치 → 통과 → VS Code · Holdings 창
    힌트 '설정'              2글자     → 막힘

**1개만 걸릴 때가 가장 안전해 보이는데 실제로는 가장 위험하다.** 그 하나가
재권님이 쓰시던 창이어도 장치를 전부 통과한다. 글자 수도 척도가 못 된다 —
한글 2글자가 영어 3글자보다 훨씬 특이하다.

    창이 쌓이는 것은 X 를 눌러 닫으면 된다.
    닫혀버린 창은 되돌릴 수 없다.

**재권님 화면에 우리 흔적을 남기지 않는다.** 이 절 전체가 그 이야기다.

#### 시험하고 나서 파일을 지우지 않는다 (2026-09-16)

**메모장은 탭으로 붙어 오래 남는다.** 시험 파일을 지우면 그 탭이 다음에 파일을
다시 읽으려다 **재권님 화면에 경고창을 띄운다.**

    C:\...\scratchpad\opentest.md 파일을 찾을 수 없습니다.   [확인]

2026-09-16 에 실제로 났고, 재권님이 스크린샷을 보내시며 "이거 뭐야?" 하고 물으셨다.

- **시험 파일은 `scratchpad` 에 만들고 그대로 둔다.** 세션 임시 폴더라
  재권님 화면에 나오지 않는다. **아무것도 안 하면 되므로 잊을 일이 없다**
- 굳이 지워야 하면 **창(탭)을 먼저 닫고 파일을 지운다.** 순서가 반대면
  메모장은 정상 동작하면서 팝업을 낸다
- 위 ② 때문에 문자는 **항상** 메모장으로 가므로, 시험할 때마다 탭이 하나씩 쌓인다
