# 회차 기록은 검사할 때마다 새 파일이 생깁니다. 저장소에 쌓을 이유가 없습니다.
# 각자 PC 에 남고, 옮기려면 폴더째 복사하면 됩니다.
history/

# latest.json 은 올립니다 (2026-09-15 지시).
# 배포 사이트에서는 버튼이 안 눌리고 마지막 결과만 읽기 전용으로 보여주는데,
# 이 파일이 저장소에 없으면 그 화면이 영영 비어 있습니다.
# 검사할 때마다 바뀌므로 커밋에 자주 딸려 나옵니다. 그건 정상입니다.
