# 재부팅 후 다시 띄우기

재부팅하거나 창을 닫아도 서버 설정과 대화는 남는다. 아래 방법으로 이어서 쓴다.

> 이 문서는 `tools/update-sessions.py` 가 만든다. 직접 고치지 말 것.
> 마지막 갱신 2026-09-16 15:21

## 재부팅하면 저절로 뜬다

`startup.bat` 이 Windows 시작프로그램에 등록되어 있다. 로그인하면 셋이 순서대로 뜬다.

| 순서 | 무엇 | 비고 |
|---|---|---|
| 1 | 로컬 서버 `8765`(holdings 미리보기) | 이미 떠 있으면 건너뛴다 |
| 2 | 브라우저 | `http://localhost:8765/` |
| 3 | 이름이 붙은 세션 창 전부 | 아래 목록 |

직접 실행해도 된다. 여러 번 실행해도 서버가 겹치지 않는다.

### 서버는 `kis_proxy.py` 다 — `holdings-preview.bat` 이 아니다

`holdings-preview.bat` 은 `http.server` 만 띄운다. 화면은 나오지만
`/api/kis/*` · `/api/dart/*` 가 전부 `404`(그런 주소 없음) 가 되어 시세도 공시도 안 나온다.
API 는 `holdings/server/kis_proxy.py` 만 갖고 있다. `startup.bat` 은 이쪽을 띄운다.

확인하려면:

```
curl http://localhost:8765/api/kis/health     200(정상) 이어야 한다
```

## 세션만 다시 열기

`resume-sessions.bat` 을 더블클릭하면 아래 세션이 각각 새 창으로 열린다.
실행할 때 목록을 다시 훑으므로, 세션이 늘어도 그대로 두면 된다.

## 지금 있는 세션

| 이름 | 세션 ID | 마지막 대화 | 크기 |
|---|---|---|---|
| **홈페이지_정리** | `b5e2504d-dc3b-43cb-b44e-ee923448b090` | 09-16 15:21 | 1MB |
| **홈페이지_정리** | `cf9e2f1b-004a-494b-92cc-a815366b4719` | 09-16 15:21 | 18MB |
| **개념정의** | `04fa6a4d-eada-4ddc-9cb9-18ae177540e4` | 09-16 15:20 | 21MB |
| **주식페이지_개발** | `36e5295a-5818-465f-ab1a-12bc8ae5f6d1` | 09-16 15:17 | 73MB |
| **스케줄_툴개발** | `9bf6d383-13c2-4a27-b4fd-ff388169b1f2` | 09-16 15:02 | 13MB |

## 하나만 열기

```
claude --resume <세션 ID>
```

예를 들어 이렇게 친다.

```
claude --resume b5e2504d-dc3b-43cb-b44e-ee923448b090    REM 홈페이지_정리
```

ID 를 모르면 아래처럼 쳐서 목록에서 고른다.

```
claude --resume
```

가장 최근 대화 하나만 이어갈 때는 이것으로 충분하다.

```
claude --continue
```

## 알아둘 것

- **이미 열려 있는 세션은 건너뛴다.** 같은 세션을 두 창에서 열면 한쪽 기록이 꼬인다.
  실행할 때마다 `--resume` 으로 떠 있는 창을 확인하므로, 재부팅 뒤가 아니어도 돌려도 된다
- **창 여는 일은 파이썬이 한다.** 배치 파일에 `start` 줄을 박아 두었더니,
  이 스크립트가 실행 도중 그 배치 파일을 다시 쓰면서 `cmd` 가 읽던 위치를 잃고
  마지막 줄을 건너뛰었다. 지금은 `--open` 으로 파이썬이 연다
- **`.bat` 안에는 한글을 쓰지 않는다.** `chcp 65001` 로 코드페이지를 바꾸면
  `cmd` 가 배치 파일의 바이트를 세는 방식이 달라진다. `cmd` 는 한 줄 실행할 때마다
  파일을 다시 열어 바이트 위치로 되짚으므로, 아래쪽에 한글이 있으면 위치가 어긋나
  **줄 중간부터 실행된다.** 2026-09-16 에 `startup.bat` 에서 이 일이 났다 —
  `'/*' 은(는) 명령이 아닙니다` 같은 조각이 쏟아지고 세션 창은 열리지 않았다.
  한글 메시지는 `tools/update-sessions.py` 가 출력한다
- **`timeout` 대신 `ping -n` 으로 기다린다.** `timeout` 은 입력이 리다이렉트된 채
  실행되면 `Input redirection is not supported` 로 즉시 죽는다
- **작업 폴더를 맞춰서 연다.** 네 세션 모두 `C:\work\KJCStudio` 에서 돈다.
  다른 곳에서 열면 상대 경로가 어긋난다. `resume-sessions.bat` 은 알아서 맞춘다
- **이름이 없는 세션은 목록에 싣지 않는다.** `/rename` 을 쓴 적이 있어야 잡힌다
- 그냥 `claude` 만 치면 **새 세션**이 생긴다. 기존 대화와 별개다
- 세션 기록은 `%USERPROFILE%\.claude\projects\C--work-KJCStudio\` 에 쌓인다
- 네 세션은 같은 작업 트리를 공유한다. 레인은 `CLAUDE.md` 「세션 역할 분담」 표를 따른다

## 목록이 안 맞을 때

세션을 새로 만들고 이름을 붙였는데 목록에 없으면 아래를 실행한다.

```
python tools\update-sessions.py
```
